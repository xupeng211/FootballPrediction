{
  "mcpServers": {
    "git": {
      "command": "python",
      "args": ["mcp_servers/git_server.py"],
      "cwd": "/home/user/projects/FootballPrediction",
      "description": "Git version control (READ-ONLY LOG) for code management",
      "note": "Only MCP server currently implemented. Other servers (postgres, redis, filesystem) are not yet implemented."
    }
  },
  "note": "This configuration has been simplified to reflect actual implementation. Only the git MCP server is available."
}
