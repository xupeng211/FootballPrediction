# CI依赖 - 包含测试运行必需的核心包
# 排除纯ML重型依赖（sklearn, joblib等）但保留必需的数据处理库

# 核心框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 数据库
sqlalchemy>=2.0.0
alembic>=1.12.0
asyncpg>=0.30.0
psycopg2-binary>=2.9.11
aiosqlite>=0.19.0

# 数据验证
pydantic>=2.5.0
email-validator>=2.0.0

# HTTP客户端
httpx>=0.25.0
requests>=2.31.0
aiohttp>=3.8.0

# 缓存
redis>=7.0.1

# 测试工具
pytest>=8.4.0
pytest-asyncio>=0.21.0
pytest-cov>=7.0.0
pytest-mock>=3.14.0
pytest-timeout>=2.1.0
pytest-xdist>=3.0.0

# 安全
passlib[bcrypt]>=1.7.4
PyJWT>=2.10.1

# 数据处理
pandas>=2.0.0
scipy>=1.10.0

# 工具
python-dotenv>=1.2.1
python-multipart>=0.0.20

# 测试覆盖率工具
coverage>=7.0.0

# 可选的轻量级工具
tenacity>=8.2.0
aiofiles>=23.2.1
psutil>=5.8.0
backoff>=2.2.0
celery>=5.3.0

# 轻量级CI: 移除重型ML依赖以避免资源超限
# joblib>=1.4.0  # 暂时注释以节省CI资源
# xgboost>=2.0.0  # 暂时注释以节省CI资源

# 监控和指标
prometheus-client>=0.19.0

# 重要：明确排除重型ML依赖
# curl_cffi (已移除内存问题版本限制)
curl_cffi>=0.5.10
# - scikit-learn (重型ML依赖，内存消耗大)
# 注：pandas/scipy已包含用于核心数据处理功能