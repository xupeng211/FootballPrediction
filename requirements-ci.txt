# CI轻量级依赖 - 只包含测试运行必需的核心包
# 排除可能导致内存问题的机器学习和重型依赖包

# 核心框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 数据库
sqlalchemy>=2.0.0
alembic>=1.12.0
asyncpg>=0.30.0
psycopg2-binary>=2.9.11

# 数据验证
pydantic>=2.5.0
email-validator>=2.0.0

# HTTP客户端
httpx>=0.25.0
requests>=2.31.0

# 缓存
redis>=7.0.1

# 测试工具
pytest>=8.4.0
pytest-asyncio>=0.21.0
pytest-cov>=7.0.0
pytest-mock>=3.14.0

# 安全
passlib[bcrypt]>=1.7.4
PyJWT>=2.10.1

# 工具
python-dotenv>=1.2.1
python-multipart>=0.0.20

# 测试覆盖率工具
coverage>=7.0.0

# 可选的轻量级工具
tenacity>=8.2.0
aiofiles>=23.2.1

# 重要：明确排除重型依赖
# - curl_cffi (可能导致内存问题)
# - pandas/numpy/scipy/scikit-learn (ML依赖，内存消耗大)
# - joblib (ML依赖)
# - prometheus-client (可选监控)