# Capability Index — 能力索引

> lifecycle: permanent
>
> 本文档是可扫描的 current-state 能力索引，不是"唯一权威"；每行尽量短，
> 权威细节指向 README canonical 表、代码合同与 current-state 文档。
> 首次建立：2026-08-01（PROJECT_KNOWLEDGE_ENTRY_AND_DOCUMENTATION_SAFETY 任务）。

## 状态词（固定词汇）

| 状态 | 含义 |
|---|---|
| CANONICAL | 正式入口存在，新工作默认使用 |
| SUPPORTED_COMPATIBILITY | 保留兼容路径，不是默认入口 |
| LEGACY | 历史遗留，不得成为新依赖 |
| BLOCKED | 入口存在或计划中，但当前未授权 / 被禁止执行 |
| NOT_ESTABLISHED | 尚未建立（设计评审完成或未开始，均未实现） |
| DOCUMENTED_ONLY | 无正式受控 canonical 入口；可能已实现并有历史执行证据（行内说明给出实现与入口），也可能仅为文档 / 审计描述 |

状态可用斜杠组合（如 CANONICAL/BLOCKED）：入口已定义但执行未授权。

## 如何使用本索引

1. 按 Domain 找到目标能力行。
2. 看 Status：CANONICAL 可复用；LEGACY 不得作为新依赖；
   BLOCKED / NOT_ESTABLISHED 不得自行实现，先确认授权。
3. 看 Canonical entrypoint / Core implementation：用入口，不要绕行。
4. 看 Authorization & side effects：执行前确认是否已获授权。
5. 看 Legacy or forbidden alternatives：避免重复造轮子或依赖历史脚本。
6. 每行状态与授权可能变化；以 README canonical 表和 current-state 文档为准。

## Domain: 历史赔率（M3 odds staging / import foundation）

| Domain | Capability | Status | Canonical entrypoint | Core implementation | Primary tests | Authorization & side effects | Legacy or forbidden alternatives | Current notes |
|---|---|---|---|---|---|---|---|---|
| Odds | 离线 source inventory | DOCUMENTED_ONLY | `npm run odds:staging:dry-run`（internal 执行：node `scripts/ops/odds_staging_dry_run.js`；未登记 README canonical 表、无 make `data-*` wrapper） | `src/infrastructure/odds_staging/sourceManifest.js` | tests/unit/odds_staging_pipeline.test.js | no-write 默认；读本地 CSV + manifest | 历史 CSV 直接入库脚本 | 离线 source inventory 已合并（M3 里程碑，Issue #1793）；入口未登记正式 canonical 表面 |
| Odds | 离线 staging pipeline | DOCUMENTED_ONLY | `npm run odds:staging:dry-run`（internal 执行，同上） | `src/infrastructure/odds_staging/pipeline.js` | tests/unit/odds_staging_pipeline.test.js、tests/unit/odds_staging_cli.test.js、tests/integration/odds_staging/ | no-write 默认（dry-run）；写文件需显式授权 | — | football-data-csv@1.3.0（M3-R2：C 系列→closing / 普通系列→first_collection_after_market_open，仅当 manifest 声明 applicable:true 且 contract_id/provider_id 与 `footballDataProviderContract.js` 冻结合同精确匹配且 season ≥ 2019/20；fail closed）+ E0/Premier League + three-season + exact-alias + Europe/London 合同（docs/M3_D4F_READINESS_REVIEW.md）；入口未登记正式 canonical 表面 |
| Odds | dry-run importer | DOCUMENTED_ONLY | `npm run odds:staging:dry-run`（internal 执行，同上） | 同上（CLI 默认 dry-run） | tests/unit/odds_staging_cli.test.js | 不写 DB、不写文件 | — | 唯一离线导入入口，默认 fail-closed/no-write；入口未登记正式 canonical 表面 |
| Odds | 离线确定性重建（M3-R1/M3-R2） | DOCUMENTED_ONLY | `npm run odds:staging:rebuild`（node `scripts/ops/odds_staging/historical_odds_rebuild.js`；同 dry-run 分类，未登记 README canonical 表） | `scripts/ops/odds_staging/historical_odds_rebuild.js` + 同级 canonical 模块 `historical_odds_rebuild_canonical.js`（只复用 runOfflineStaging / emitDeterministicResult，不重实现 staging） | tests/unit/odds_staging_rebuild.test.js、tests/unit/odds_staging_rebuild_canonical.test.js、tests/unit/odds_staging_provider_contract.test.js（M3-R2 新增）；canonical 单元套件 8,436/8,436 pass | no-write 默认；bundle / emit-dir / candidates 必须全部在 Git 仓库之外且 emit-dir 已存在为空；只写 --emit-dir；无网络无 DB；`--canonical-history` 经有界只读 git 子进程从不可变 git 对象恢复三个固定来源（commit/path → blob → SHA-256 校验，无预备 bundle）；`--validate` 从发射输出重算每个收据事实（含 emitted_digest、固定契约交叉核验、temporal 事实与派生语义、provider_semantic_contract provenance pinning、series_semantics_distribution 投影）；收据不含任何期望计数常量 | — | M3-R1 有界重建：3 个 historical_git_recovery 来源（raw_odds_2223 / raw_odds_2324 / real_odds_raw）一次确定性重建为 repo-external 发射输出 + 收据（schema m3-historical-odds-rebuild-receipt/v2）；BUILD_A/BUILD_B 字节一致、receipt verify PASS ×2 + tamper probe REJECTED（acceptance-remediation-20260809T113656Z/，见 docs/PROJECT_STATUS.md M3-R1 节）；收据业务哈希 40b02195…（M3-R1 定义组合，07e579ed… 遗留组合不可复算）。**M3-R2**：收据 schema v3（+provider_semantic_contract / series_semantics_distribution / 7 维 evaluation_readiness）；官方 provider 合同对账（closing / first_collection_after_market_open，per-source 判定）；population 不变量与业务哈希不变；readiness = NOT_READY_FOR_TEMPORAL_VALUE_EVALUATION（closing 语义 YES / exact timestamp NO / strict decision-time NO / benchmark YES）；BUILD_A/B verify valid ×2 + 6/6 tamper probes REJECTED（footballprediction-m3-r2-provider-temporal-contract-20260809T162422Z/，见 docs/PROJECT_STATUS.md M3-R2 节）；入口未登记正式 canonical 表面 |
| Golden Dataset | GD-A01 spine + historical-odds file-first assembler | SPECIALIZED / INTERNAL | `npm run gd:a01 -- build|validate`（所有输入/输出绝对路径显式指定；仓库外输出） | `src/infrastructure/golden_dataset/GdA01AssemblyContract.js` + `GdA01ArtifactContract.js` + `GdA01Assembler.js`；`scripts/ops/gd_a01_assembler.js` | `tests/unit/gd_a01_assembler.test.js` + affected canonical/FotMob/odds/linkage suites | 完全离线；只读校验显式 canonical/FotMob/M3 artifacts；不访问 DB、不联网、不写 raw、无 features/training；GD-A01 branch implementation awaits merge | 不得替代 M3 rebuild、FotMob capture/staging、matchLinker 或 provider contract；不得作为 GD-A02+ / Golden Dataset / training frame | Deterministic spine + odds foundation；real frozen profile admits 888 with source-unmatched/ambiguous admitted set = 0；provider-defined closing benchmark ≠ exact timestamp；`GOLDEN_DATASET_COMPLETE=NO` |
| Golden Dataset | GD-A02 FotMob facts projection | SPECIALIZED / INTERNAL | `npm run gd:a02 -- build|validate`（所有输入/输出绝对路径显式指定；仓库外输出） | `src/infrastructure/golden_dataset/GdA02FactsContract.js` + `GdA02Assembler.js`；`scripts/ops/gd_a02_assembler.js`；contract: `docs/data/GD_A02_FACTS_CONTRACT.md` | `tests/unit/gd_a02_assembler.test.js` + affected GD-A01/FotMob parser/staging/linkage suites | 完全离线；复用 GD-A01 population/linkage、FotMob capture/staging contracts；不访问 DB、不联网、不写 raw、不训练、不做 backtest；invalid evidence 显式 rejected/quarantined，禁止 silent drop/default zero | 不得创建 parallel match linker/parser；不得把 postmatch facts 当 prematch features；不得替代 GD-A03+ / training frame | Deterministic five-section + result/xG facts artifact；`POSTMATCH_ONLY`；`STRICT_DECISION_TIME_READY=NO`；`REAL_TRAINING_READINESS=NOT_READY`；不等于 `GOLDEN_DATASET_COMPLETE` |
| Golden Dataset | GD-A03 prior-state feature view | SPECIALIZED / INTERNAL | `npm run gd:a03 -- build|validate`（V1 artifact；所有输入/输出绝对路径显式指定；仓库外输出） | `src/infrastructure/golden_dataset/GdA03PriorStateContract.js` + `GdA03ArtifactContract.js` + `GdA03PriorStateAssembler.js`；V-next historical projection: `src/infrastructure/standings/PremierLeagueFrozenEvidenceAdapter.js` + `src/infrastructure/golden_dataset/GdA03StandingsIntegration.js`；contract: `docs/data/GD_A03_PRIOR_STATE_FEATURE_CONTRACT.md`；versioned registry: `config/model_feature_contracts.json` | `tests/unit/gd_a03_assembler.test.js` + `tests/unit/standings_gd_a03_integration.test.js` + GD-A01/GD-A02/canonical identity regression suites + `tests/unit/ml/test_vnext_feature_contract.py`；外部 audit: `footballprediction-standings-gd-a03-integration-v1-20260817/` | V1 assembler 行为保持不变；V-next historical path 完全离线，先证明 canonical 1,140-fixture schedule closure，再调用唯一纯 standings engine；不访问 DB、不联网、不写 raw/L3、不训练、不做 backtest；audit projection 不是 canonical V1 artifact、training frame 或 runtime cache | 不得使用 SchemaManager proxy/default、l3_features、当前比赛 postmatch facts 或 parallel linker/parser；不得复制 points/GD/GF/ranking；不得替代 decision-time evidence 或训练授权；V-next 只定义不激活 | V1 prior-state foundation remains historical/default；V-next = 17-feature versioned contract, non-activated；standings historical projection proven `887/888` with engine parity `888/888`；`TARGET_KICKOFF_EXCLUSIVE`；SOT/raw ELO pending closure；runtime source remains NOT_PROVEN；possession unavailable；`STRICT_DECISION_TIME_VALUE_EVALUATION=NOT_READY`；`GOLDEN_DATASET_COMPLETE=NO`；runtime numeric parity remains unproven |
| Standings | Premier League point-in-time pure semantic engine | SPECIALIZED / INTERNAL | 无独立 runtime 入口；由历史 frozen-evidence adapter / 仓库外 audit harness 调用 | `src/infrastructure/standings/StandingsContractBinding.js` + `PointInTimeStandingsEngine.js` + `PremierLeagueFrozenEvidenceAdapter.js` + `src/infrastructure/canonical/StableValue.js`；contract: `config/model_feature_contracts.json` | `tests/unit/standings_semantic_engine.test.js` + `tests/unit/standings_gd_a03_integration.test.js`；外部 audits: `footballprediction-standings-engine-implementation-v1-20260817/`、`footballprediction-standings-gd-a03-integration-v1-20260817/` | 纯内存、确定性、无文件/网络/DB/环境/墙钟依赖；历史 adapter 先证明 1,140 fixture closure；只接受已绑定的 Premier League v1 normalized input；888 target projection 不写 canonical GD-A03 | 不得复用 SchemaManager estimated proxy/default；不得改写 `GdA03PriorStateAssembler.buildTablePositionLine`；不得创建 runtime provider/parallel ranking engine；不得把 audit projection 当 training/runtime artifact | `standings/premier-league-point-in-time/v1` 已实现；GD-A03 V-next historical projection `887/888` 已 proven；runtime eligibility、training eligibility、V-next activation 均保持 NO |
| Odds | semantic identity | DOCUMENTED_ONLY | pipeline 内（经 odds:staging:dry-run 执行，无独立入口） | `src/infrastructure/odds_staging/footballDataIdentity.js` | tests/unit/odds_staging_match_identity.test.js | 纯计算 | — | 实现与测试存在（M3 交付），随未登记入口执行 |
| Odds | match linking | DOCUMENTED_ONLY | pipeline 内（同上） | `src/infrastructure/odds_staging/matchLinker.js` | 见 odds_staging 测试族 | 只读比对 | 人工 / 历史模糊匹配脚本 | 888 exact / 4 kickoff conflicts / 248 canonical-only / 252 无 exact link（docs/data/FOTMOB_CURRENT_STATE.md） |
| Odds | idempotency | DOCUMENTED_ONLY | pipeline 内（同上） | `src/infrastructure/odds_staging/deduplication.js` | tests/unit/odds_staging_historical.test.js | 只读 | — | D4E 稳定重放 0 accepted / 0 quarantine / 9 duplicates 零表差（docs/PROJECT_STATUS.md） |
| Odds | duplicate policy | DOCUMENTED_ONLY | pipeline 内（同上） | `src/infrastructure/odds_staging/deduplication.js` | tests/unit/odds_staging_historical.test.js | 只读 | — | 重复向量检测与 quarantine 决策 |
| Odds | quarantine 分类 | DOCUMENTED_ONLY | pipeline 内（同上） | `src/infrastructure/odds_staging/persistenceRepository.js` / `persistenceContracts.js` | tests/unit/odds_staging_persistence_contract.test.js | 只读；写库仅限授权 sandbox | — | D4B 冻结合同：38,616 accepted / 216 quarantined（docs/PROJECT_STATUS.md） |
| Odds | kickoff conflict 处理 | DOCUMENTED_ONLY | pipeline 内（同上） | `matchLinker.js` + `deduplication.js` | 见 odds_staging 测试族 | 只读 | — | 4 个冲突（3×15min + 1×30min），linkage-quarantine 分离（docs/data/FOTMOB_CURRENT_STATE.md、docs/M3_D4F_READINESS_REVIEW.md） |
| Odds | snapshot_type 合同 / 显式 opening-current-closing 映射 | DOCUMENTED_ONLY | pipeline 内（同上） | `src/infrastructure/odds_staging/contracts.js` + `adapters.js` | tests/unit/odds_staging_pipeline.test.js、tests/unit/odds_staging_historical.test.js | 纯计算 | — | `ALLOWED_SNAPSHOT_TYPES = opening / current / closing / unknown`；只有来源显式提供 opening/current/closing 字段时才使用对应类型，否则保持 unknown |
| Odds | 普通列 / C 列推断 opening-closing | BLOCKED | 无 | 无（禁止实现） | tests/unit/odds_staging_historical.test.js（断言保持 unknown） | 禁止推断 | 按行序、列顺序或普通/C 列命名推断的旧思路 | 来源没有时间证据必须保持 unknown；禁止把普通列解释为 opening、把 C 列解释为 closing（contracts.js、adapters.js 注释与测试） |
| Odds | staging schema | DOCUMENTED_ONLY | 无（唯一应用路径 = `make m3-odds-sandbox-migrate`，固定 M3-D4D-B1 sandbox 范围，需 `ALLOW_M3_PERSISTENT_SANDBOX_MIGRATION=1` + 精确授权短语） | `database/migrations/V26.8__create_odds_historical_staging_contract.sql`、`V26.9__add_odds_historical_observation_fingerprint.sql` | tests/integration/odds_staging/ephemeral_postgres.test.js 等 | 需双授权 + 单独授权；M3 迁移仅在授权 sandbox / disposable tmpfs 库应用过 | 直接执行 migration apply（禁止）；`make data-schema-*` 不覆盖 V26.8/V26.9（`data-schema-plan` 仅列到 V26.4，`data-schema-migrate` 未接通执行） | M3 合同迁移（docs/PROJECT_STATUS.md、docs/M3_ODDS_STAGING_PERSISTENT_SANDBOX_RUNBOOK.md） |
| Odds | controlled synthetic write（D4E） | DOCUMENTED_ONLY | 无（历史执行证据：`make m3-odds-sandbox-d4e-preflight/-write/-replay/-conflict-probe/-quarantine-conflict-probe`，需 `ALLOW_M3_D4E_PERSISTENT_SANDBOX_WRITE=1` + 精确授权短语） | `src/infrastructure/odds_staging/persistenceRepository.js` | tests/unit/odds_staging_d4e_controlled_write.test.js | 只接受确定性 synthetic 输入；持久 sandbox（M3-D4D-B1）范围；未经新授权不得执行 | 不得指向 `data-schema-m3-canonical-inventory-disposable-*`（那是 canonical inventory proof：迁移 V26.10 + CanonicalInventoryWriter，不运行 D4E 的 V26.8/V26.9 赔率 staging 写入） | D4E = 1 run / 1 source / 6 accepted / 3 quarantine，已结案（docs/PROJECT_STATUS.md、docs/M3_ODDS_STAGING_PERSISTENT_SANDBOX_RUNBOOK.md） |
| Odds | production import schema | NOT_ESTABLISHED | 无 | 无（historical odds → production 表的 import 集成未实现） | — | 无 | 历史 import 脚本 | 与 canonical inventory writer 分开：writer/V26.10 合同/disposable proof 已存在（PR #1811），本行仅指 historical odds staging → production bookmaker odds / matches 表的正式 import 集成、授权表面与执行流程，仍 NOT_ESTABLISHED；真实持久化/生产写入仍 BLOCKED（docs/PROJECT_STATUS.md） |
| Odds | 真实历史赔率 import | BLOCKED | 无 | 无 | — | 未授权、未执行 | — | 三份 Git 审计输入已离线读取（docs/M3_D4F_READINESS_REVIEW.md L549-551 / L631-634）：`raw_odds_2223.csv`（380 行 / 13,680 obs）、`raw_odds_2324.csv`（380 行 / 12,546 obs）、`real_odds_raw.csv`（420 行 / 12,606 obs），合计 1,180 行 / 38,832 obs；D4B 的 38,616 accepted / 216 quarantined 是历史合同证据，不是 D4F 审计人口。从未正式 import 或写入任何数据库 / 生产；真实导入需后续单独授权，勿只恢复单份文件却按总量规划 |

## Domain: FotMob

| Domain | Capability | Status | Canonical entrypoint | Core implementation | Primary tests | Authorization & side effects | Legacy or forbidden alternatives | Current notes |
|---|---|---|---|---|---|---|---|---|
| FotMob | candidate exporter | CANONICAL/BLOCKED | 仅 `make data-fotmob-candidates-network-export`（需 NETWORK_AUTHORIZATION=yes） | `scripts/ops/fotmob_candidates_export.js` → `src/infrastructure/fotmob/FotMobCandidateExporter.js` | tests/unit/fotmob_candidate_exporter.test.js | 每次实际执行都会访问 FotMob 网络（CLI 对非 --help 调用强制 `--network-preview=true` + `--network-authorization=yes`，普通直接调用被 CLI 阻止）；`OUTPUT=<dir>` 经 Makefile 转发为 `--output`，写入候选 artifact（identity-v1：`candidate-match-identity.v1.json` + `.summary.json`，要求仓库外已存在的绝对目录）——文件写盘与网络授权相互独立、需另行授权（ACTIVE_MILESTONE 停止边界禁止 artifact 写盘）；`--retain-raw-responses` 落盘是另一项可选行为；不写数据库；当前未获得真实网络执行授权 | `n3_live_fotmob_raw_retain.js`、`pageprops_v2_*`、`adg60_*`、`titan_discovery.js` 及其它 Phase/ADG 历史采集脚本 | 唯一正式路径；不存在"本地无网络导出"模式；PR #1813 后支持 canonical-v2 |
| FotMob | status mapping | CANONICAL | 同上（CLI 内） | `src/infrastructure/fotmob/FotMobStatusContract.js` | tests/unit/fotmob_candidate_exporter.test.js | 纯映射 | 历史脚本各自硬编码状态 | unknown / started fail closed（PR #1813） |
| FotMob | raw retention | DOCUMENTED_ONLY | 无受控入口（`make data-fotmob-candidates-network-export` 默认 identity-v1，不转发 `--output-schema` / `--retain-raw-responses`；canonical-v2 参数仅可经直接 CLI 调用且 `--retain-raw-responses` 为 REQUIRED） | `FotMobCandidateExporter.js` | tests/unit/fotmob_candidate_exporter.test.js | 实现存在（PR #1813）；本地文件写盘；both-or-neither 语义；git revision 绑定；执行仍需网络授权 | `n3_live_fotmob_raw_retain.js`（网络/UPSERT 路径，LEGACY） | 不得保存 / 打印完整 HTML body 到对话（AGENTS.md）；当前无受控执行表面触发该能力 |
| FotMob | capture manifest | DOCUMENTED_ONLY | 无受控入口（依赖 canonical-v2，同 raw retention 行） | `FotMobCandidateExporter.js`（SHA-256 manifest） | tests/unit/fotmob_candidate_exporter.test.js | 实现存在（PR #1813）；本地文件写盘；both-or-neither 语义 | — | raw + manifest 必须同生共灭（PR #1813）；当前无受控执行表面触发该能力 |
| FotMob | identity projection hash | DOCUMENTED_ONLY | 无受控入口（依赖 canonical-v2，同 raw retention 行） | `FotMobCandidateExporter.js` + `CanonicalInventoryContract.js` | tests/unit/fotmob_candidate_exporter.test.js | 实现存在（PR #1813）；纯计算 | — | 稳定 payload hash，不含 volatile fetch metadata（AGENTS.md）；当前无受控执行表面触发该能力 |
| FotMob | business hash | DOCUMENTED_ONLY | 无受控入口（依赖 canonical-v2，同 raw retention 行） | 同上（full business hash） | tests/unit/fotmob_candidate_exporter.test.js、tests/unit/canonical_inventory_contract.test.js | 实现存在（PR #1813）；纯计算 | — | 与 identity projection hash 双哈希并存；当前无受控执行表面触发该能力 |
| FotMob | canonical inventory contract | CANONICAL | `make data-m3-canonical-inventory-preflight ARTIFACT=<path> ARTIFACT_SHA256=<sha256>`（no-write 合同验证，不依赖网络导出） | `src/infrastructure/canonical/CanonicalInventoryContract.js`（`readOrdinaryArtifact()` → `validateArtifactDocument()`，经 `scripts/ops/canonical_inventory_writer.js`） | tests/unit/canonical_inventory_contract.test.js | 校验失败即 fail closed；只验证外部提供的 hash-bound artifact，无网络 / DB 副作用 | — | 与"生成 v2 artifact"（raw retention 等，仍无受控入口）分开：验证 v2 artifact 有受控 preflight（PR #1813 / README canonical 表） |
| FotMob | canonical writer proof | CANONICAL（proof 范围） | `make data-m3-canonical-inventory-preflight` / `make data-m3-canonical-inventory-disposable-proof` | `src/infrastructure/canonical/CanonicalInventoryWriter.js` | tests/unit/canonical_inventory_writer.test.js、canonical_inventory_authorization.test.js、canonical_inventory_operator.test.js | preflight 只读；disposable proof 仅确定性 synthetic 输入 + 专用 tmpfs 库 | — | persistent canonical write 仍 blocked（README canonical 表） |
| FotMob | v1 compatibility | SUPPORTED_COMPATIBILITY | `make data-fotmob-candidates-network-export` 默认输出（identity-v1，输出路径不变） | `FotMobCandidateExporter.js` | tests/unit/fotmob_candidate_exporter.test.js | 受控入口默认行为；执行仍需网络授权 | — | P3-3 v1 paired-write 弱点未修复、不属于阶段A（PR #1813 Debt Impact） |
| FotMob | production acquisition | NOT_ESTABLISHED | 无 | 无 | — | 无 | 上述 legacy 采集脚本 | README canonical 表：Not yet established for production acquisition |
| FotMob | real capture | BLOCKED | 无 | 无 | — | 未授权；FOTMOB_REAL_CAPTURE_READINESS 为 planning milestone（Issue #1793 / PR #1813） | — | 有界两路径兼容性 probe 已完成（2 次请求；match detail 路由 / EPL fixtures 路由均兼容、未见 access-control 信号）；公共条款审查已完成（written permission 缺失）；三赛季目标人口 888 采集已于 GDI1C 显式授权下完成（2026-08-10/11，ACQUISITION_NETWORK_PHASE=CLOSED，FOTMOB_RAW_ACQUISITION_STATUS=COMPLETE；详见 docs/data/FOTMOB_CURRENT_STATE.md closeout 块）；任何**新**采集仍须单独授权 |
| FotMob | detail capture plan | CANONICAL | `make data-fotmob-detail-capture-plan CANDIDATE_ARTIFACT=<外部绝对路径> SEASON="YYYY/YYYY ..." [MATCH_ID=...] [LIMIT=n] OUTPUT=<外部绝对路径>`（离线；直接 Node CLI `scripts/ops/fotmob_detail_capture.js plan ...` 为 internal engine，非 canonical 接口） | `src/infrastructure/fotmob/FotMobDetailCapturePlan.js` + `FotMobDetailCaptureContract.js` | tests/unit/fotmob_detail_capture_plan.test.js、fotmob_detail_capture_remediation.test.js | 离线纯计算；输出到仓库外绝对非 symlink 路径；要求显式选择（--season/--match-id/--limit 至少其一），绝不默认全量 | 历史采集脚本 | 确定性 plan（fotmob-detail-capture-plan/v1 + plan_business_sha256）；hash 由 PLAN 构建器与 CAPTURE 校验器共享的 validateAndRecomputeCapturePlan 重算（无 derive-and-accept fallback）；输入 artifact v1/v2 双 hash 校验 |
| FotMob | detail capture (real network) | BLOCKED | `make data-fotmob-detail-capture-execute`（需 PLAN/EXPECTED_PLAN_SHA256/AUTHORIZATION_ID/MAX_REQUESTS/DELAY_MS/OUTPUT_ROOT/RUN_ID + NETWORK_AUTHORIZATION=yes + CONFIRM_REAL_FOTMOB_DETAIL_CAPTURE=1 + CONFIRM_MAX_FOTMOB_REQUESTS===MAX_REQUESTS；任何变量缺失在 make 层、Node 之前失败）；离线前置：`make data-fotmob-detail-capture-preflight`（零 fetch / 零写：重算 plan hash、校验全部非 execute 授权门、打印候选数与 URL 摘要） | `src/infrastructure/fotmob/FotMobDetailCapturePipeline.js` + `FotMobDetailCaptureRetention.js` | tests/unit/fotmob_detail_capture_pipeline.test.js、fotmob_detail_capture_retention.test.js、fotmob_detail_capture_remediation.test.js | 默认关闭：--execute + CONFIRM_REAL_FOTMOB_DETAIL_CAPTURE=1 + CONFIRM_MAX_FOTMOB_REQUESTS + authorization-id + expected-plan-sha256 + clean worktree + 40-hex HEAD + 仓库外非 symlink 输出根 + 非 symlink plan + 安全 run-id；单 URL 合同（https://www.fotmob.com/match/<digits>，GET、不跟随重定向、无 cookie/auth/proxy/browser、并发 1、重试 0、delay≥60s、30s 超时）；19 项内容有效性门（含可信 observed-match-id 来源与冲突检测 fail closed）；稳定 allowlisted payload（fotmob-match-detail-capture-payload/v1，不落盘原始 HTML）+ manifest 配对原子保留；run-bound 不可变 plan snapshot 在任何网络请求前写入；resume 全面绑定校验；attempted 计数先于 fetch（失败/超时请求计入计数）；不写 DB；真实执行未授权 | `n3_live_fotmob_raw_retain.js`（LEGACY 网络/UPSERT 路径） | 每次真实执行都会访问 FotMob 网络；冻结的 888 人口采集已于 GDI1C 显式授权下执行（2026-08-10/11），任何**新**采集当前仍未获得授权（OWNER_REAL_CAPTURE_AUTHORIZATION=NO 语义保留于新采集）；测试全部 mock（REAL_NETWORK_FORBIDDEN_IN_TEST） |
| FotMob | detail capture replay (offline) | CANONICAL | `make data-fotmob-detail-capture-replay RUN_DIR=<外部绝对路径>/runs/<run-id>`（离线；直接 Node CLI `scripts/ops/fotmob_detail_capture.js replay ...` 为 internal engine） | `src/infrastructure/fotmob/FotMobDetailCaptureRetention.js` | tests/unit/fotmob_detail_capture_retention.test.js、fotmob_detail_capture_cli.test.js、fotmob_detail_capture_remediation.test.js | 完全离线；run plan snapshot REQUIRED（缺失 fail closed）；payload file hash + manifest self-hash 必须匹配；parsed_at 取自捕获记录 → 重复 replay 字节一致；候选身份仅来自 run snapshot、绝不来自文件名；symlink 全级别拒绝；零网络零 DB | — | 确定性结构化 artifact（fotmob-match-detail-artifact/v1）；输出到 run 目录 replay/ 子目录 |
| FotMob | detail staging (offline converter/validator) | CANONICAL | `make data-fotmob-detail-staging-{help,receipt,build,validate}`（容器优先 `$(COMPOSE_DEV) exec -T dev`；`-build` 需 SOURCE_INDEX + OUTPUT_ROOT；`-validate` 需 OUTPUT_ROOT，可选 `--expected-latest-marker-sha256=<64hex>` 或 `--anchor-checkpoint=<外部绝对路径>` 锚定，二者互斥；`-receipt`/`-build` 可选 `LIMITS_FILE=<仓库外绝对路径 JSON>` 在硬上限内覆盖归档资源限额（repository-external、严格校验）；直接 Node CLI `scripts/ops/fotmob_detail_staging.js` 为 internal engine） | `src/infrastructure/fotmob/FotMobDetailStaging{Contract,Converter,Retention}.js` + `FotMobDetailStagingSourceVerification.js` | tests/unit/fotmob_detail_staging_{contract,converter,retention,cli,source_verification}.test.js | 完全离线（OFFLINE ONLY / ZERO NETWORK / ZERO DATABASE / NO MIGRATION / NO CAPTURE）：无 fetcher/DB client import、无墙钟进业务字段（generated_at=manifest response_received_at（记录为 artifact source_response_received_at）；observation_id=确定性 RFC 4122 UUIDv5）；输出仅限仓库外绝对非 symlink 路径；O_NOFOLLOW fd 读取 + dev/inode 校验（P1-4 TOCTOU 缓解，诚实威胁模型：不抵御同 uid 持续竞争）；受控私有输出目录（owner 检查、拒绝 group/world writable）；append-only 文件快照 + 编号 store-state-<seq>.json 账本版本；per-file 原子写（O_EXCL tmp+fsync+同 fs rename）+ 独占 per-store lock；冲突 fail-closed；LOGICAL_COMMIT_MARKER 唯一提交点（residue 报告不当作已提交）；`-validate` 重验 artifact/summary/store ledger 并检测 partial output，MODE_1_UNANCHORED / MODE_2_EXTERNALLY_ANCHORED；不写 DB、无 migration、不执行 capture | — | 确定性 artifact（fotmob-detail-staging-artifact/v1）：5 个 versioned JSONB sections 逐字节保留、canonical_match_id 恒 null + UNLINKED_NOT_ATTEMPTED（不猜测 link）；直接复用 pipeline 捕获 hashing（computeStableCapturePayloadSha256，无自实现副本）；16 场归档（one/five/ten-match pilot archives）两次 build + 两次 validate 字节一致，派生输出已删除；marker_event（parser 注入 AddedTime/Half 分钟标记、无 id 设计）为合法变体；PR #1817 阻塞修复：commit-marker 原子提交/回滚、VERIFIED_PACKAGE_RECEIPT（真实 archive SHA-256 + 纯 Node tar reader）、A–E 38 项 validator（篡改检测）、REPEAT_EQUIVALENT 重建、LAYER_A/LAYER_B 完整性 hash；SC-002 状态：ENFORCEMENT_INFRASTRUCTURE=COMPLETE / STAGING_PRODUCTION_ROLE_DEPLOYMENT=PENDING / PR1817_CHANGES_SC002=NO；PR #1817 已于 2026-08-06 squash 合并入 main（merge commit `fd60117d2…`，post-merge main Production Gate 31075669344 success） |

## Domain: 数据库与迁移

| Domain | Capability | Status | Canonical entrypoint | Core implementation | Primary tests | Authorization & side effects | Legacy or forbidden alternatives | Current notes |
|---|---|---|---|---|---|---|---|---|
| DB | forward schema definition authority | CANONICAL/BLOCKED | 新 schema change 只进 `database/migrations/` 的 reviewed `V*.sql`；执行仍走既有 `make data-schema-*` / 专项受控 surface | `database/migrations/V*.sql`；机器契约：`config/db_schema_authority.json` | `tests/unit/db_schema_authority_convergence.test.js`、SC-002 migration policy tests | canonical location 不等于 execution authorization；默认 startup 不自动执行；DB/migration execution 仍逐项单独授权 | Alembic revision、SchemaManager DDL、runtime `CREATE TABLE`、`init_db.sql` bootstrap 均不得承载未来 schema change | 当前现代合同到 V26.10；V26.8/V26.9/V26.10 各自保留既有 sandbox/disposable gate |
| DB | M3 SQL migration 树（V26.8 / V26.9 / V26.10） | DOCUMENTED_ONLY | V26.10：`make data-schema-m3-canonical-inventory-disposable-*`（唯一接通路径）；V26.8 / V26.9：无（仅 `make m3-odds-sandbox-migrate` sandbox 路径，双授权） | `database/migrations/V*.sql`（V26.8 / V26.9 / V26.10 为 M3 合同） | tests/integration/canonical_inventory/disposable_postgres.test.js 等 | 执行迁移需门禁 + 单独授权；M3 迁移仅在 disposable tmpfs 库 / 授权 sandbox 应用过 | 直接 psql 执行 migration apply（禁止）；`make data-schema-plan` 止于 V26.4、`data-schema-migrate` 未接通（不覆盖 V26.8 / V26.9） | M3 具体执行 surface 仍按既有 contract；authority 只回答未来定义位置 |
| DB | Alembic migration 树 | LEGACY | 无（历史 compatibility/static inspection only；future revisions frozen） | `src/database/migrations/`（head `003_v145`，3 个版本） | `tests/unit/test_sc002_alembic_migration_guard_design.py`、`test_sc002_alembic_migration_runtime_guard.py` | 不得作为未来 schema authority；不得默认启动执行；显式 execution 仍需单独授权 | 新增 Alembic revision、`alembic upgrade` 作为默认启动动作（禁止） | 旧树不覆盖当前 V26.4–V26.10 合同，保留历史不代表当前 authority |
| DB | SchemaManager mutation | LEGACY | 无；`initialize_schema()` / `initialize_production_schema()` 已停用 | `src/database/schema_manager.py`（read-only/introspection helpers retained；`align_external_ids` / `bulk_insert_features` 为 legacy data-DML compatibility methods） | `tests/unit/db_schema_authority_convergence.test.js`、`tests/unit/test_python_runtime_guard_phase2c_batch2_static.py` | 不得从 canonical/default entrypoint 调用 mutation methods；legacy DML 不是 schema authority | 以 SchemaManager DDL 作为 schema upgrade / production initialization（禁止） | 历史 DDL 正文保留供审计，入口 fail-fast；legacy DML 不得承载 future schema change |
| DB | L3 Elo child helper | SPECIALIZED / INTERNAL | 无；仅由 L3 stitch 的 legacy child path 调用 | `scripts/maintenance/recalculate_elo.js` | `tests/unit/db_schema_authority_convergence.test.js`、L3 tests | `schema_mutation_entrypoint=disabled`；只读检查 `public.team_elo_ratings`，要求 schema 已预置；future schema changes forbidden，Elo 数据写入仍需单独授权 | 不得把该 helper 当作 forward schema authority 或默认 startup migration；不得在此新增 runtime DDL | L3 主 pipeline 与 Elo child 均为 pre-provisioned-schema data paths；当前 raw SQL authority 未找到 `team_elo_ratings` 定义，provisioning gap 作为 carry-forward |
| DB | Docker init bootstrap | DOCUMENTED_ONLY | 仅 `docker-compose.dev.yml` 的开发 DB 空卷 bootstrap | `deploy/docker/init_db.sql` | `tests/unit/db_schema_authority_convergence.test.js`、`test_runtime_db_role_permission_dev_poc.py` | dev-only；不代表 forward authority；执行仍需单独授权 | unified/production-like Compose 挂载或生产/staging migration authority（禁止） | 文件内容冻结；schema drift 作为 carry-forward |
| DB | DB write guard | CANONICAL | 仅覆盖显式 import 并调用该 helper 的 scripts/ops JS 脚本（精确枚举：`grep -rlE "require\([^)]*db_write_guard|import[^;]*db_write_guard" scripts/ops --include="*.js" | grep -vE "helpers/db_write_guard\.js$|db_write_guard_static_enforcement_dry_run\.js$"`（排除已内嵌于命令，当前 53 个）；CI 侧由 AI Workflow Gate 的 db_write_guard enforcement check——`scripts/ops/helpers/db_write_guard_advisory_check.py` / `scripts/ops/db_write_guard_static_enforcement_dry_run.js`——对扫描器判定为直接写入风险且未被跳过的 new/modified 脚本强制接入；`read_only_or_false_positive`（无写风险关键字）与 `skipped_complex`（Phase1 跳过 / allowlist / browser 自动化）分类不报 violation） | `scripts/ops/helpers/db_write_guard.js`（fail-closed；guard 自述仅供 ops scripts 调用，不构成统一 writer 层、不替代脚本自身检查） | 见 guard 相关测试 | 显式调用者未通过 guard 时被阻止（需 `DRY_RUN=false`〔默认 true〕+ `ALLOW_DB_WRITE=yes` + `FINAL_DB_WRITE_CONFIRMATION=yes` + 表级门禁；CREATE / ALTER / DELETE / TRUNCATE / DROP 等高风险操作另需 `ALLOW_SCHEMA_WRITE=yes`） | 未导入该 guard 的写路径不受其约束、各有自身授权机制：`scripts/ops/run_production.js` → `ProductionHarvester.saveData` → `Persistence.dualSave`（INSERT/UPDATE raw_match_data）；src/ 内 CanonicalInventoryWriter、FixtureRepository 等 | 仅覆盖显式调用者，不是全局环境门禁；`grep -l` 类宽松查询会混入治理脚本 / helper 自身 / 扫描器，勿用于枚举；审计任意写路径时须逐路径确认授权（AGENTS.md §5.3） |
| DB | controlled write（sandbox） | DOCUMENTED_ONLY | 无（历史执行证据：`make m3-odds-sandbox-d4e-*` 固定表面） | `src/infrastructure/odds_staging/persistenceRepository.js` | tests/unit/odds_staging_d4e_controlled_write.test.js | 需双授权 + 单独授权；持久 sandbox（M3-D4D-B1）范围；未经新授权不得执行 | — | D4E 已结案，保留证据 1/1/6/3（docs/PROJECT_STATUS.md、docs/M3_ODDS_STAGING_PERSISTENT_SANDBOX_RUNBOOK.md） |
| DB | production DB write | BLOCKED | 无 | 无 | — | 未授权 | — | 任何写库命令默认 blocked（AGENTS.md §5.3） |

## Domain: 数据采集 / 赔率收割 / 特征构建（L1/L2 / odds:harvest / l3:stitch）

| Domain | Capability | Status | Canonical entrypoint | Core implementation | Primary tests | Authorization & side effects | Legacy or forbidden alternatives | Current notes |
|---|---|---|---|---|---|---|---|---|
| Data | L1 发现 / 种子写入（data-l1-*） | CANONICAL/BLOCKED | `make data-l1-*`（`make data-help` 列出；preview → plan → authorization → preflight → execute 多阶段） | Makefile Phase 5.05-5.07L1 目标 | — | preview / plan 阶段 no-write；authorization / execute 阶段需每目标显式授权（如 `USER_AUTHORIZED_MATCHES_SEED_COMMIT=yes` + `FINAL_HUMAN_CONFIRMATION=yes`） | `scripts/ops/titan_discovery.js`（legacy/admin-only，不得作为新依赖，AGENTS.md §6.1） | 未授权不执行（README canonical 表） |
| Data | L2 原始比赛数据（data-l2-*） | CANONICAL/BLOCKED | `make data-l2-*`（`make data-help` 列出；preview → plan → authorization → preflight → write 多阶段） | Makefile L2 目标 | — | ingest / write 阶段需逐目标显式授权（如 ALLOW_DB_WRITE 门禁）+ 最终人工确认 | 历史 raw 直写脚本 | 未授权不执行（README canonical 表） |
| Odds | 赔率收割（odds:harvest） | DOCUMENTED_ONLY | `npm run odds:harvest`（无 make `data-*` 受控门禁 wrapper；README canonical 表登记为 Primary canonical，但 npm script 直接宿主运行 Node，绕过容器化多阶段门禁） | `scripts/ops/odds_harvest_pipeline.js` | — | 默认 l3Enabled=true：收割后触发 `l3_stitch_pipeline.js`（L3_STITCH_FULL_RECALCULATE=true），其运行 pre-provisioned-schema 的 `scripts/maintenance/recalculate_elo.js`，写 matches / l3_features / team_elo_ratings（默认跨域写入链）；赔率-only 需 `--skip-l3`；可能访问网络并写 DB；执行前确认授权 / 环境 / 凭据（README L198） | `npm run odds:sniper` 为 Specialized/Internal 替代 | 已实现（脚本存在）但无受控入口；README 登记 Primary canonical 与本索引 DOCUMENTED_ONLY 的差异即有无 make `data-*` 门禁；未授权不执行 |
| Data | L3 特征构建 | DOCUMENTED_ONLY | `npm run l3:stitch`（无 make `data-*` 受控门禁 wrapper；README canonical 表登记为 Primary canonical，但 npm script 直接宿主运行 Node，绕过容器化多阶段门禁；`make data-l3-*` 是另一套 fixture/local pipeline） | `scripts/ops/l3_stitch_pipeline.js` | — | 执行前确认输入数据与写入范围；L3 与其 Elo child 都要求预置 schema，仅写 l3_features / team_elo_ratings 数据，不执行 runtime DDL（README L201） | `npm run smelt` 为 Specialized/Internal 替代 | 已实现（脚本存在）但无受控入口；未授权不执行 |

## Domain: 训练 / 预测 / 回测

| Domain | Capability | Status | Canonical entrypoint | Core implementation | Primary tests | Authorization & side effects | Legacy or forbidden alternatives | Current notes |
|---|---|---|---|---|---|---|---|---|
| ML | canonical per-prediction model as-of temporal contract | CANONICAL/BLOCKED | 无 runtime 入口；future prediction context 必须由 canonical contract binding 校验 | `config/model_feature_contracts.json` (`decision_boundaries.model_as_of`) + `src/ml/inference/model_asof_contract.py` + `src/ml/inference/feature_contract_boundary_validator.py` + `src/ml/inference/feature_contract_registry.py`；contract: `docs/data/MODEL_ASOF_CONTRACT.md` | `tests/unit/ml/test_model_asof_contract.py` + affected feature-contract/standings/odds regression suites | 纯校验、无网络/DB/文件写入/墙钟依赖；freeze 不实现 runtime source/capture，不启动 training/backtest；生产使用仍 blocked | 第二套 temporal/feature registry、generated_at/kickoff/captured_at 偷换 T、provider-defined closing 伪装 exact decision-time odds | `canonical-model-asof/v1`；Owner Option C；`FEATURE_AS_OF_UTC=MODEL_DECISION_TIME_UTC`；strict value evaluation remains `NOT_READY`；V1 20 active/default、V-next 17 not activated |
| ML | canonical runtime capture contract / pure replay validation | CANONICAL/BLOCKED | 无 live capture 入口；future capture package 必须显式绑定 prediction context、selected evidence 与 payload bytes | `config/model_feature_contracts.json` (`decision_boundaries.runtime_capture`) + `src/ml/inference/runtime_capture_contract.py`；contract: `docs/data/RUNTIME_CAPTURE_CONTRACT.md` | `tests/unit/ml/test_runtime_capture_contract.py` + model-as-of/registry/GD-A03 regression suites | 纯内存结构、时间关系、content-digest 与 payload integrity 校验；无文件/网络/DB/墙钟依赖；不实现 storage/pipeline/provider；生产使用仍 blocked | 第二套 capture registry、captured_at 偷换 observed_at、event_time 偷换 availability、额外 evidence 偷换 selected evidence、caller Git SHA 伪造 provenance | `canonical-runtime-capture/v1`；`CAPTURE_MUST_BE_LTE_T`；manifest self-excluding canonical digest；structural/content replay 可验证，但 source normalization/numeric/train replay 均 `NOT_PROVEN` |
| ML | training | CANONICAL/BLOCKED | `npm run train -- --input <offline-feature-frame> --output <candidate-path>`（`train:fast` / `train:deep` 变体） | `src/ml/training/canonical_training_producer.py` + `src/ml/inference/canonical_model_loader.py` | `tests/unit/ml/test_canonical_training_producer.py`、canonical loader/contract/readiness tests | 仅接受显式离线特征帧；按 canonical 20-feature contract 做 chronological/OOS 训练；只写显式 non-production candidate；仅显式训练授权后可执行；不改 manifest | `scripts/ops/train_model.py`（TITAN/legacy 兼容路径）、`scripts/model_training/train_baseline_v1.py`（legacy DB/random-split 路径） | PR-6 建立安全 producer；生产 artifact 仍 pending/null，activation 仍需单独授权 |
| ML | canonical API verified loading + read-only model observability | CANONICAL/BLOCKED | `/predict` 启动/lifespan canonical load attempt（当前 pending，保持 503）；`GET /api/v1/models/info`、`GET /api/v1/models/list` 为只读状态面 | `src/ml/inference/canonical_model_loader.py` + `src/ml/inference/model_dispatcher.py` + `src/api/model_management.py` | `tests/unit/ml/test_canonical_model_loader.py`、`tests/unit/api/test_model_management.py`、artifact/readiness、predict/health 回归、PR-4 lifecycle boundary tests | 只消费 manifest/feature contract/readiness；不训练、不激活 artifact、不写 DB、不 fetch；未来 active artifact 仍需单独授权；不提供 reload/switch/rollback/upload/backup | 旧 admin/retraining router 与 orphan `src/core/inference_engine.py` 已在 PR-4 移除；旧 model-management mutation/discovery surface 已由 PR-5 替换为只读 canonical router；`src/ml/inference/model_loader.py` / `predictor.py` 仅为非 API 策略兼容路径，`titan_loader.py` 为非 canonical CLI consumer，均不得作为 canonical API loader | PR-3 establishes verify-before-deserialize, exact manifest↔contract binding, shared readiness, and startup load-ready wiring; PR-5 mounts sanitized read-only observability backed by the same authorities; current `config/model_artifacts.json` API row remains pending |
| ML | prediction | CANONICAL/BLOCKED | `npm run predict -- --input <json-file>`（JSON stdin；`predict:json` 输出变体；`predict:dry` 仅输入校验） | `src/ml/inference/prediction_runtime.py` + `src/ml/inference/predict_cli.py` + `src/ml/inference/model_dispatcher.py` + `src/ml/inference/canonical_model_loader.py` | `tests/unit/ml/test_prediction_authority_convergence.py`、HTTP 503/route 回归、canonical loader/contract tests | 默认 CLI 是显式 payload 的薄适配器；不执行 legacy business-DB batch discovery、不 fetch、不写 artifact；canonical artifact pending/null 时稳定失败并且不回退 Titan/mini；仍需单独 prediction authorization | `npm run predict:titan-legacy` → `scripts/ops/predict_pipeline.py` → `titan_loader` + `prediction_repo`；`scripts/ops/titan_cruise_control.py` 为历史 Titan caller，均非 canonical | HTTP 与 CLI 共用同一 process-local canonical owner；legacy DB/Titan 特征不转换为 canonical 20-feature contract |
| ML | backtest | NOT_ESTABLISHED | 无 | 无 | — | 无 | `recon_scanner.js`、`gold_pilot_50.js`、`titan_marathon.js` 不是 canonical 回测入口 | 需未来业务里程碑实现并验收（README canonical 表） |
| ML | 离线概率基准（VALUE_MVP-1，offline probability benchmark） | DOCUMENTED_ONLY | 无 canonical 入口（internal research evaluation：`scripts/model_training/value_mvp_baseline_vs_closing.py`；未登记 README canonical 表） | `src/ml/value_mvp/`（pipeline / validator / protocol / leakage / market） | tests/unit/ml/value_mvp/（76 pass）+ 全套 2692 收集 | 纯离线只读：zero DB / zero network / zero new data；no-write 默认；不得做 decision-time / ROI / 盈利 / CLV 声明 | 生产回测 / profit simulator / betting engine（禁止命名与实现） | 回答 "prematch baseline vs closing 1X2 market 是否有竞争力可预测信息"；protocol 冻结 c6716e91…；real OOS RUN_A/B 字节一致、pooled n=511 delta log loss 0.1268463858（CI [0.0846640358, 0.1691520672]）→ **MARKET_BETTER_THAN_MODEL**（合法结果）；validator 17 项 + 3 个 tamper 探针（含 coordinated CI 伪造）；Codex R1/R2 P0=P1=P2=0；README Backtest 行保持 Not yet established（docs/PROJECT_STATUS.md VALUE_MVP-1 节） |

## 本索引不回答什么

- 不回答具体命令的完整用法 —— 看 README canonical 表与各 CLI 的 `--help`。
- 不回答授权当前是否存在 —— 授权逐项确认；任何文档记录都不等于动态验证。
- 不回答里程碑进度 —— 看 docs/ACTIVE_MILESTONE.md。
- 不代替代码与数据合同 —— 合同以 `database/migrations/V*.sql` 与 contract 模块为准。
- 不把"存在 Makefile target"当作"已接通能力" —— 以 README canonical 表与 current-state 文档为准。

## 维护触发条件

- README canonical 表增删入口时同步本索引。
- 任何状态变化（NOT_ESTABLISHED → CANONICAL、BLOCKED 解除、新能力落地）时更新对应行。
- 新增能力前先查本索引；发现缺失即补行，同时声明新组件的生命周期（AGENTS.md §2.5）。
