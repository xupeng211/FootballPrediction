# Data Collection Service 配置文件

service:
  name: "Data Collection Service"
  description: "数据采集服务微服务"
  port: 8002
  version: "1.0.0"

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

database:
  host: "localhost"
  port: 5432
  name: "footballprediction_data_collection"
  user: "postgres"
  password: "password"

redis:
  host: "localhost"
  port: 6379
  db: 0

monitoring:
  enabled: true
  metrics_path: "/metrics"
  health_path: "/health"

security:
  cors_enabled: true
  authentication_required: false
