# Minimal environment configuration for testing

# Database configuration
DB_HOST=db
DB_PORT=5432
DB_NAME=footballprediction_test
DB_USER=postgres
DB_PASSWORD=testpassword

# API configuration
API_HOST=0.0.0.0
API_PORT=8000

# Minimal mode settings
FAST_FAIL=false
MINIMAL_HEALTH_MODE=true
MINIMAL_API_MODE=true
ENABLE_METRICS=false

# Python settings
PYTHONPATH=/app