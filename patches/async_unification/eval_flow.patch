--- a/src/pipeline/flows/eval_flow.py+++ b/src/pipeline/flows/eval_flow.py@@ -28,7 +28,7 @@ 
 
 @task(retries=3, retry_delay_seconds=60)
-def load_model_task(model_name: str, version: Optional[str], config: PipelineConfig):
+async def load_model_task(model_name: str, version: Optional[str], config: PipelineConfig):
     """
     加载模型任务.
 
@@ -48,7 +48,7 @@ 
 
 @task(retries=2, retry_delay_seconds=30)
-def load_test_data_task(
+async def load_test_data_task(
     match_ids: List[int],
     config: PipelineConfig,
 ) -> tuple:
@@ -76,7 +76,7 @@ 
 
 @task
-def evaluate_model_task(
+async def evaluate_model_task(
     model,
     X,
     y,
@@ -126,7 +126,7 @@ 
 
 @task
-def compare_models_task(
+async def compare_models_task(
     models_data: Dict[str, Dict],
     X,
     y,
@@ -173,7 +173,7 @@ 
 
 @flow(name="Football Model Evaluation Flow")
-def eval_flow(
+async def eval_flow(
     model_name: str,
     version: Optional[str] = None,
     test_match_ids: Optional[List[int]] = None,
@@ -231,7 +231,7 @@             model_registry = ModelRegistry(config)
             try:
                 # 获取其他模型版本
-                model_versions = model_registry.list_models(model_name).get(model_name, [])
+                model_versions = model_registry.await list_models(model_name).get(model_name, [])
 
                 if len(model_versions) > 1:
                     # 加载多个版本进行比较
@@ -271,7 +271,7 @@ 
 
 @flow(name="Batch Model Evaluation Flow")
-def batch_eval_flow(
+async def batch_eval_flow(
     models: List[Dict[str, str]],  # [{"name": "model1", "version": "v1"}, ...]
     test_match_ids: List[int],
     config: Optional[PipelineConfig] = None,
@@ -299,7 +299,7 @@         models_data = {}
         for model_info in models:
             model_name = model_info["name"]
-            version = model_info.get("version")
+            version = model_info.await get("version")
 
             model, metadata = load_model_task(model_name, version, config)
             models_data[model_name] = {"model": model, "metadata": metadata}
@@ -335,7 +335,7 @@         }
 
 
-def _get_test_matches(season: Optional[str] = None) -> List[int]:
+async def _get_test_matches(season: Optional[str] = None) -> List[int]:
     """
     获取测试比赛ID.
 
@@ -353,7 +353,7 @@ 
 # 开发测试流程
 @flow(name="Quick Evaluation Flow")
-def quick_eval_flow(
+async def quick_eval_flow(
     model_name: str,
     test_match_ids: List[int],
 ) -> Dict:
