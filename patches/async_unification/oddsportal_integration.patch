--- a/src/collectors/oddsportal_integration.py+++ b/src/collectors/oddsportal_integration.py@@ -22,13 +22,13 @@ class OddsPortalIntegration:
     """OddsPortal集成器."""
 
-    def __init__(self, config_path: str = "config/oddsportal_config.yaml"):
+async def __init__(self, config_path: str = "config/oddsportal_config.yaml"):
         """初始化OddsPortal集成."""
         self.config = self._load_config(config_path)
         self.scraper = OddsPortalScraper()
         self.logger = logger
 
-    def _load_config(self, config_path: str) -> dict[str, Any]:
+async def _load_config(self, config_path: str) -> dict[str, Any]:
         """加载配置文件."""
         try:
             config_file = Path(config_path)
@@ -68,7 +68,7 @@ 
                 # 添加延迟避免被封
                 await asyncio.sleep(
-                    self.config.get("scraper", {}).get("delay_min", 1.0)
+                    self.config.await await get("scraper", {}).get("delay_min", 1.0)
                 )
 
             except Exception as e:
@@ -90,7 +90,7 @@ 
                 # 添加延迟避免被封
                 await asyncio.sleep(
-                    self.config.get("scraper", {}).get("delay_min", 1.0)
+                    self.config.await await get("scraper", {}).get("delay_min", 1.0)
                 )
 
             except Exception as e:
@@ -112,7 +112,7 @@ 
                 # 添加延迟避免被封
                 await asyncio.sleep(
-                    self.config.get("scraper", {}).get("delay_min", 1.0)
+                    self.config.await await get("scraper", {}).get("delay_min", 1.0)
                 )
 
             except Exception as e:
@@ -120,7 +120,7 @@ 
         return teams
 
-    def _convert_match_data(self, match: OddsPortalMatch) -> MatchData:
+async def _convert_match_data(self, match: OddsPortalMatch) -> MatchData:
         """转换比赛数据格式."""
         return MatchData(
             match_id=match.match_id,
@@ -132,52 +132,52 @@             score=match.score,
         )
 
-    def _convert_odds_data(self, match_id: str, odds: dict) -> OddsData:
+async def _convert_odds_data(self, match_id: str, odds: dict) -> OddsData:
         """转换赔率数据格式."""
         return OddsData(
             match_id=match_id,
-            home_win=odds.get("home_win"),
-            draw=odds.get("draw"),
-            away_win=odds.get("away_win"),
-            over_2_5=odds.get("over_2_5"),
-            under_2_5=odds.get("under_2_5"),
-            btts_yes=odds.get("btts_yes"),
-            btts_no=odds.get("btts_no"),
+            home_win=odds.await get("home_win"),
+            draw=odds.await get("draw"),
+            away_win=odds.await get("away_win"),
+            over_2_5=odds.await get("over_2_5"),
+            under_2_5=odds.await get("under_2_5"),
+            btts_yes=odds.await get("btts_yes"),
+            btts_no=odds.await get("btts_no"),
             source="oddsportal",
             collection_time=datetime.now(),
         )
 
-    def _convert_team_data(self, team: dict) -> TeamData:
+async def _convert_team_data(self, team: dict) -> TeamData:
         """转换队伍数据格式."""
         return TeamData(
-            team_id=team.get("team_id"),
-            name=team.get("name"),
-            country=team.get("country"),
-            founded=team.get("founded"),
-            stadium=team.get("stadium"),
+            team_id=team.await get("team_id"),
+            name=team.await get("name"),
+            country=team.await get("country"),
+            founded=team.await get("founded"),
+            stadium=team.await get("stadium"),
         )
 
 
 class OddsPortalAdapter(DataSourceAdapter):
     """OddsPortal数据源适配器."""
 
-    def __init__(self):
+async def __init__(self):
         super().__init__("oddsportal", priority=1)
         self.integration = OddsPortalIntegration()
 
     async def fetch_matches(self, **kwargs) -> list[MatchData]:
         """获取比赛数据."""
-        match_ids = kwargs.get("match_ids", [])
+        match_ids = kwargs.await get("match_ids", [])
         return await self.integration.collect_match_data(match_ids)
 
     async def fetch_odds(self, **kwargs) -> list[OddsData]:
         """获取赔率数据."""
-        match_ids = kwargs.get("match_ids", [])
+        match_ids = kwargs.await get("match_ids", [])
         return await self.integration.collect_odds_data(match_ids)
 
     async def fetch_teams(self, **kwargs) -> list[TeamData]:
         """获取队伍数据."""
-        team_ids = kwargs.get("team_ids", [])
+        team_ids = kwargs.await get("team_ids", [])
         return await self.integration.collect_team_data(team_ids)
 
     async def health_check(self) -> bool:
@@ -192,12 +192,12 @@ 
 
 # 工厂函数
-def create_oddsportal_adapter() -> OddsPortalAdapter:
+async def create_oddsportal_adapter() -> OddsPortalAdapter:
     """创建OddsPortal适配器实例."""
     return OddsPortalAdapter()
 
 
-def create_oddsportal_integration(
+async def create_oddsportal_integration(
     config_path: str = "config/oddsportal_config.yaml",
 ) -> OddsPortalIntegration:
     """创建OddsPortal集成实例."""
