--- a/src/decorators/service.py+++ b/src/decorators/service.py@@ -23,7 +23,7 @@     pass  # 添加pass语句
     """装饰器服务,管理和应用装饰器"""
 
-    def __init__(self, factory: DecoratorFactory | None = None):
+async def __init__(self, factory: DecoratorFactory | None = None):
         """函数文档字符串."""
         # 添加pass语句
         self.factory = factory or DecoratorFactory()
@@ -31,21 +31,21 @@         self._global_decorators: list[DecoratorConfig] = []
         self._function_decorators: dict[str, list[DecoratorConfig]] = {}
 
-    def load_configuration(self, config_path: str | Path) -> None:
+async def load_configuration(self, config_path: str | Path) -> None:
         """加载装饰器配置文件."""
         self.factory.load_config_from_file(config_path)
 
         # 加载全局装饰器
         for _name, config in self.factory._config_cache.items():
-            if config.conditions and config.conditions.get("global", False):
+            if config.conditions and config.conditions.await get("global", False):
                 self._global_decorators.append(config)
 
-    def register_global_decorator(self, config: DecoratorConfig) -> None:
+async def register_global_decorator(self, config: DecoratorConfig) -> None:
         """注册全局装饰器."""
         self._global_decorators.append(config)
         self.factory._config_cache[config.name] = config
 
-    def register_function_decorator(
+async def register_function_decorator(
         self, function_name: str, config: DecoratorConfig
     ) -> None:
         """为特定函数注册装饰器."""
@@ -54,7 +54,7 @@         self._function_decorators[function_name].append(config)
         self.factory._config_cache[config.name] = config
 
-    def apply_decorators(
+async def apply_decorators(
         self, func: Callable, decorator_names: list[str] | None = None, **kwargs
     ) -> Callable:
         """应用装饰器到函数."""
@@ -103,7 +103,7 @@         # 返回包装后的函数
         return self._create_wrapper(decorator_component)
 
-    def _matches_function(self, func_name: str, patterns: list[str]) -> bool:
+async def _matches_function(self, func_name: str, patterns: list[str]) -> bool:
         """检查函数名是否匹配模式."""
         for pattern in patterns:
             if pattern.endswith("*"):
@@ -126,7 +126,7 @@                     return True
         return False
 
-    def _create_wrapper(self, decorator_component: DecoratorComponent) -> Callable:
+async def _create_wrapper(self, decorator_component: DecoratorComponent) -> Callable:
         """创建函数包装器."""
 
         async def wrapper(*args, **kwargs):
@@ -140,7 +140,7 @@             context.add_execution_step(decorator_component.get_name())
 
             # 执行装饰器组件
-            return await decorator_component.execute(*args, **kwargs)
+            return await decorator_component.await execute(*args, **kwargs)
 
         # 保留原函数的元数据
         wrapper.__name__ = decorator_component.func.__name__
@@ -153,13 +153,13 @@ 
         return wrapper
 
-    def get_function_stats(self, func_name: str) -> dict[str, Any] | None:
+async def get_function_stats(self, func_name: str) -> dict[str, Any] | None:
         """获取函数的装饰器统计信息."""
         if func_name in self._decorated_functions:
             return self._decorated_functions[func_name].get_all_stats()
         return None
 
-    def get_all_stats(self) -> dict[str, Any]:
+async def get_all_stats(self) -> dict[str, Any]:
         """获取所有装饰器的统计信息."""
         stats = {"total_functions": len(self._decorated_functions), "functions": {}}
 
@@ -168,7 +168,7 @@ 
         return stats
 
-    def clear_stats(self) -> None:
+async def clear_stats(self) -> None:
         """清空所有统计信息."""
         for component in self._decorated_functions.values():
             for decorator in component.decorators:
@@ -177,7 +177,7 @@                 decorator.total_execution_time = 0.0
                 decorator.last_execution_time = None
 
-    def reload_configuration(self) -> None:
+async def reload_configuration(self) -> None:
         """重新加载配置."""
         # 清空缓存
         self._global_decorators.clear()
@@ -185,15 +185,15 @@ 
         # 从工厂重新加载
         for config in self.factory._config_cache.values():
-            if config.conditions and config.conditions.get("global", False):
+            if config.conditions and config.conditions.await get("global", False):
                 self._global_decorators.append(config)
 
 
 # 便捷装饰器函数
-def decorate(decorator_names: list[str] | None = None, **decorator_kwargs) -> Callable:
+async def decorate(decorator_names: list[str] | None = None, **decorator_kwargs) -> Callable:
     """装饰器工厂函数,用于装饰其他函数."""
 
-    def decorator(func: Callable) -> Callable:
+async def decorator(func: Callable) -> Callable:
         # 获取装饰器服务
         service = DecoratorService()
 
@@ -205,7 +205,7 @@     return decorator
 
 
-def with_logging(
+async def with_logging(
     level: str = "INFO", log_args: bool = True, log_result: bool = True, **kwargs
 ) -> Callable:
     """添加日志装饰器."""
@@ -218,7 +218,7 @@     )
 
 
-def with_retry(
+async def with_retry(
     max_attempts: int = 3, delay: float = 1.0, backoff_factor: float = 2.0, **kwargs
 ) -> Callable:
     """添加重试装饰器."""
@@ -231,7 +231,7 @@     )
 
 
-def with_metrics(
+async def with_metrics(
     metric_name: str | None = None, tags: dict[str, str] | None = None, **kwargs
 ) -> Callable:
     """添加指标装饰器."""
@@ -243,19 +243,19 @@     )
 
 
-def with_cache(ttl: int | None = None, **kwargs) -> Callable:
+async def with_cache(ttl: int | None = None, **kwargs) -> Callable:
     """添加缓存装饰器."""
     return decorate(decorator_names=["default_cache"], ttl=ttl, **kwargs)
 
 
-def with_timeout(timeout_seconds: float = 30.0, **kwargs) -> Callable:
+async def with_timeout(timeout_seconds: float = 30.0, **kwargs) -> Callable:
     """添加超时装饰器."""
     return decorate(
         decorator_names=["default_timeout"], timeout_seconds=timeout_seconds, **kwargs
     )
 
 
-def with_all(
+async def with_all(
     log_level: str = "INFO",
     retry_attempts: int = 3,
     cache_ttl: int | None = None,
