--- a/src/models/prediction_model.py+++ b/src/models/prediction_model.py@@ -52,7 +52,7 @@     Prediction Model Base Class (Stub Implementation)
     """
 
-    def __init__(self, model_name: str, model_type: str = "classification"):
+async def __init__(self, model_name: str, model_type: str = "classification"):
         """函数文档字符串."""
         # 添加pass语句
         """
@@ -76,7 +76,7 @@         }
         self.logger.info(f"PredictionModel initialized: {model_name}")
 
-    def train(self, X: pd.DataFrame, y: pd.Series, **kwargs) -> dict[str, Any]:
+async def train(self, X: pd.DataFrame, y: pd.Series, **kwargs) -> dict[str, Any]:
         """训练模型.
 
         Args:
@@ -112,7 +112,7 @@         )
         return metrics
 
-    def predict(self, X: pd.DataFrame) -> np.ndarray:
+async def predict(self, X: pd.DataFrame) -> np.ndarray:
         """预测.
 
         Args:
@@ -135,7 +135,7 @@ 
         return predictions
 
-    def predict_proba(self, X: pd.DataFrame) -> np.ndarray:
+async def predict_proba(self, X: pd.DataFrame) -> np.ndarray:
         """预测概率.
 
         Args:
@@ -155,7 +155,7 @@ 
         return proba
 
-    def evaluate(self, X: pd.DataFrame, y: pd.Series) -> dict[str, float]:
+async def evaluate(self, X: pd.DataFrame, y: pd.Series) -> dict[str, float]:
         """评估模型.
 
         Args:
@@ -181,7 +181,7 @@ 
         return metrics
 
-    def save_model(self, file_path: str) -> bool:
+async def save_model(self, file_path: str) -> bool:
         """保存模型.
 
         Args:
@@ -208,7 +208,7 @@             self.logger.error(f"Failed to save model: {e}")
             return False
 
-    def load_model(self, file_path: str) -> bool:
+async def load_model(self, file_path: str) -> bool:
         """加载模型.
 
         Args:
@@ -233,7 +233,7 @@             self.logger.error(f"Failed to load model: {e}")
             return False
 
-    def get_feature_importance(self) -> dict[str, float]:
+async def get_feature_importance(self) -> dict[str, float]:
         """获取特征重要性.
 
         Returns:
@@ -251,7 +251,7 @@ 
         return importance
 
-    def explain_prediction(self, X: pd.DataFrame) -> dict[str, Any]:
+async def explain_prediction(self, X: pd.DataFrame) -> dict[str, Any]:
         """解释预测结果.
 
         Args:
@@ -288,14 +288,14 @@     Football Prediction Model (Stub Implementation)
     """
 
-    def __init__(self, model_name: str = "football_predictor"):
+async def __init__(self, model_name: str = "football_predictor"):
         """函数文档字符串."""
         # 添加pass语句
         super().__init__(model_name, "classification")
         self.prediction_type = PredictionType.MATCH_RESULT
         self.target_classes = ["home_win", "draw", "away_win"]
 
-    def predict_match(
+async def predict_match(
         self, home_team: str, away_team: str, features: dict[str, Any]
     ) -> dict[str, Any]:
         """预测比赛结果.
@@ -330,7 +330,7 @@ 
         return result
 
-    def batch_predict(self, matches: list[dict[str, Any]]) -> list[dict[str, Any]]:
+async def batch_predict(self, matches: list[dict[str, Any]]) -> list[dict[str, Any]]:
         """批量预测.
 
         Args:
@@ -342,9 +342,9 @@         results = []
         for match in matches:
             prediction = self.predict_match(
-                match.get("home_team", ""),
-                match.get("away_team", ""),
-                match.get("features", {}),
+                match.await get("home_team", ""),
+                match.await get("away_team", ""),
+                match.await get("features", {}),
             )
             results.append(prediction)
 
@@ -355,18 +355,18 @@ _model_registry: dict[str, PredictionModel] = {}
 
 
-def register_model(model: PredictionModel) -> None:
+async def register_model(model: PredictionModel) -> None:
     """注册模型."""
     _model_registry[model.model_name] = model
     logging.getLogger(__name__).info(f"Model registered: {model.model_name}")
 
 
-def get_model(model_name: str) -> PredictionModel | None:
+async def get_model(model_name: str) -> PredictionModel | None:
     """获取模型."""
-    return _model_registry.get(model_name)
-
-
-def list_models() -> list[str]:
+    return _model_registry.await get(model_name)
+
+
+async def list_models() -> list[str]:
     """列出所有模型."""
     return list(_model_registry.keys())
 
