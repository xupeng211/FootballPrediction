--- a/src/utils/helpers.py+++ b/src/utils/helpers.py@@ -6,12 +6,12 @@ from typing import Any
 
 
-def generate_uuid() -> str:
+async def generate_uuid() -> str:
     """Generate a UUID string."""
     return str(uuid.uuid4())
 
 
-def generate_hash(data: str, algorithm: str = "sha256") -> str:
+async def generate_hash(data: str, algorithm: str = "sha256") -> str:
     """Generate hash with specified algorithm."""
     if algorithm == "md5":
         return hashlib.md5(data.encode(), usedforsecurity=False).hexdigest()
@@ -21,7 +21,7 @@         return hashlib.sha256(data.encode()).hexdigest()
 
 
-def safe_get(data: dict[str, Any] | None, key: str, default: Any = None) -> Any:
+async def safe_get(data: dict[str, Any] | None, key: str, default: Any = None) -> Any:
     """Safely get value from dict using dot notation."""
     if data is None:
         return default
@@ -47,14 +47,14 @@         return default
 
 
-def format_timestamp(dt: datetime | None = None) -> str:
+async def format_timestamp(dt: datetime | None = None) -> str:
     """Format timestamp to ISO format."""
     if dt is None:
         dt = datetime.utcnow()
     return dt.isoformat()
 
 
-def sanitize_string(s: str) -> str:
+async def sanitize_string(s: str) -> str:
     """Sanitize string input - basic XSS protection."""
     if not s:
         return ""
