--- a/src/monitoring/metrics_collector_enhanced.py+++ b/src/monitoring/metrics_collector_enhanced.py@@ -25,19 +25,19 @@     """简化的增强指标收集器."""
 
     @classmethod
-    def initialize(cls):
+async def initialize(cls):
         """初始化指标收集器."""
         logger.info("✅ MetricsCollector initialized successfully")
 
-    def __init__(self):
+async def __init__(self):
         """初始化增强指标收集器."""
         self.metrics = {}
 
-    def collect(self) -> dict[str, Any]:
+async def collect(self) -> dict[str, Any]:
         """收集指标."""
         return {"timestamp": datetime.utcnow(), "metrics": self.metrics}
 
-    def add_metric(self, name: str, value: Any):
+async def add_metric(self, name: str, value: Any):
         """添加指标."""
         self.metrics[name] = value
         logger.debug(f"Added metric: {name} = {value}")
@@ -46,11 +46,11 @@ class MetricsAggregator:
     """指标聚合器 - 简化版本."""
 
-    def __init__(self):
+async def __init__(self):
         """初始化指标聚合器."""
         self.aggregated_metrics = {}
 
-    def aggregate(self, metrics: dict[str, Any]):
+async def aggregate(self, metrics: dict[str, Any]):
         """聚合指标."""
         for key, value in metrics.items():
             if key in self.aggregated_metrics:
@@ -62,7 +62,7 @@                 self.aggregated_metrics[key] = value
         logger.debug(f"Aggregated {len(metrics)} metrics")
 
-    def get_aggregated(self) -> dict[str, Any]:
+async def get_aggregated(self) -> dict[str, Any]:
         """获取聚合后的指标."""
         return self.aggregated_metrics
 
@@ -70,13 +70,13 @@ class MetricPoint:
     """指标点 - 简化版本."""
 
-    def __init__(self, name: str, value: float, timestamp: datetime | None = None):
+async def __init__(self, name: str, value: float, timestamp: datetime | None = None):
         """初始化指标点."""
         self.name = name
         self.value = value
         self.timestamp = timestamp or datetime.utcnow()
 
-    def to_dict(self) -> dict[str, Any]:
+async def to_dict(self) -> dict[str, Any]:
         """转换为字典."""
         return {"name": self.name, "value": self.value, "timestamp": self.timestamp}
 
@@ -85,7 +85,7 @@ _collector = None
 
 
-def get_metrics_collector() -> EnhancedMetricsCollector:
+async def get_metrics_collector() -> EnhancedMetricsCollector:
     """获取全局指标收集器实例."""
     global _collector
     if _collector is None:
@@ -93,7 +93,7 @@     return _collector
 
 
-def track_prediction_performance(prediction_id: str, accuracy: float):
+async def track_prediction_performance(prediction_id: str, accuracy: float):
     """函数文档字符串."""
     pass  # 添加pass语句
     """跟踪预测性能"""
@@ -102,7 +102,7 @@     logger.info(f"Tracked prediction performance: {prediction_id} - {accuracy}")
 
 
-def track_cache_performance(cache_name: str, hit_rate: float):
+async def track_cache_performance(cache_name: str, hit_rate: float):
     """函数文档字符串."""
     pass  # 添加pass语句
     """跟踪缓存性能"""
