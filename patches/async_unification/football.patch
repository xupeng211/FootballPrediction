--- a/src/adapters/football.py+++ b/src/adapters/football.py@@ -55,7 +55,7 @@ class ApiFootballAdapter(FootballDataAdapter):
     """API Football适配器 - 使用Football-Data.org API."""
 
-    def __init__(self, name: str = "Football-Data.org"):
+async def __init__(self, name: str = "Football-Data.org"):
         """初始化API适配器."""
         super().__init__(name)
         self.base_url = "https://api.football-data.org/v4"
@@ -130,7 +130,7 @@                     "limit": 500,  # 增加限制以获取更多比赛
                 }
 
-                response = await client.get(url, headers=self.headers, params=params)
+                response = await client.await get(url, headers=self.headers, params=params)
 
                 if response.status_code != 200:
                     error_msg = f"API请求失败: HTTP {response.status_code}"
@@ -141,7 +141,7 @@                     raise FootballAdapterConnectionError(error_msg)
 
                 data = response.json()
-                matches = data.get("matches", [])
+                matches = data.await get("matches", [])
 
                 if not matches:
                     logger.warning(f"联赛 {league_code} 赛季 {season} 没有找到比赛数据")
@@ -174,7 +174,7 @@             async with httpx.AsyncClient(timeout=30.0) as client:
                 url = f"{self.base_url}/competitions/{league_code}/teams"
 
-                response = await client.get(url, headers=self.headers)
+                response = await client.await get(url, headers=self.headers)
 
                 if response.status_code != 200:
                     raise FootballAdapterConnectionError(
@@ -182,7 +182,7 @@                     )
 
                 data = response.json()
-                return data.get("teams", [])
+                return data.await get("teams", [])
 
         except httpx.ConnectError as e:
             raise FootballAdapterConnectionError(f"网络连接失败: {e}")
@@ -213,7 +213,7 @@                     )
 
                 data = response.json()
-                return data.get("competitions", [])
+                return data.await get("competitions", [])
 
         except httpx.ConnectError as e:
             raise FootballAdapterConnectionError(f"网络连接失败: {e}")
