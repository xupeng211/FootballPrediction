--- a/src/data/processing/football_data_cleaner.py+++ b/src/data/processing/football_data_cleaner.py@@ -27,11 +27,11 @@ class FootballDataCleaner:
     """足球数据清洗器 - 完整实现."""
 
-    def __init__(self, config: dict | None = None):
+async def __init__(self, config: dict | None = None):
         self.config = config or self._get_default_config()
         self.data_quality_report = {}
 
-    def _get_default_config(self) -> dict:
+async def _get_default_config(self) -> dict:
         return {
             "remove_duplicates": True,
             "handle_missing": True,
@@ -42,7 +42,7 @@             "missing_strategy": "adaptive",
         }
 
-    def clean_dataset(
+async def clean_dataset(
         self, raw_data: pd.DataFrame, data_type: str = "matches"
     ) -> pd.DataFrame:
         """清洗完整数据集.
@@ -93,7 +93,7 @@         logger.info(f"数据清洗完成，最终数据形状: {cleaned_data.shape}")
         return cleaned_data
 
-    def _validate_basic_structure(
+async def _validate_basic_structure(
         self, data: pd.DataFrame, data_type: str
     ) -> pd.DataFrame:
         """基础数据结构验证."""
@@ -105,7 +105,7 @@ 
         # 检查必需列是否存在
         missing_columns = []
-        for col in required_columns.get(data_type, []):
+        for col in required_columns.await get(data_type, []):
             if col not in data.columns:
                 missing_columns.append(col)
 
@@ -114,7 +114,7 @@ 
         return data
 
-    def _remove_duplicates(self, data: pd.DataFrame, data_type: str) -> pd.DataFrame:
+async def _remove_duplicates(self, data: pd.DataFrame, data_type: str) -> pd.DataFrame:
         """移除重复数据."""
         initial_count = len(data)
 
@@ -146,7 +146,7 @@ 
         return data_dedup
 
-    def _handle_missing_values_adaptive(self, data: pd.DataFrame) -> pd.DataFrame:
+async def _handle_missing_values_adaptive(self, data: pd.DataFrame) -> pd.DataFrame:
         """自适应缺失值处理."""
         missing_info = {}
 
@@ -174,7 +174,7 @@ 
         return data
 
-    def _determine_missing_strategy(
+async def _determine_missing_strategy(
         self, column: str, series: pd.Series, missing_ratio: float
     ) -> str:
         """确定缺失值处理策略."""
@@ -187,7 +187,7 @@         else:
             return "mode"
 
-    def _apply_missing_strategy(self, series: pd.Series, strategy: str) -> pd.Series:
+async def _apply_missing_strategy(self, series: pd.Series, strategy: str) -> pd.Series:
         """应用缺失值处理策略."""
         if strategy == "drop_column":
             return series  # 将在上层处理
@@ -225,7 +225,7 @@             else:
                 return series.fillna(0)
 
-    def _detect_and_handle_outliers(
+async def _detect_and_handle_outliers(
         self, data: pd.DataFrame, data_type: str
     ) -> pd.DataFrame:
         """检测和处理异常值."""
@@ -253,7 +253,7 @@ 
         return data
 
-    def _detect_outliers_iqr(self, series: pd.Series) -> pd.Series:
+async def _detect_outliers_iqr(self, series: pd.Series) -> pd.Series:
         """使用IQR方法检测异常值."""
         try:
             q1 = series.quantile(0.25)
@@ -269,7 +269,7 @@             logger.error(f"IQR异常值检测失败: {e}")
             return pd.Series(False, index=series.index)
 
-    def _handle_outliers(
+async def _handle_outliers(
         self, data: pd.DataFrame, column: str, outliers: pd.Series
     ) -> pd.DataFrame:
         """处理异常值."""
@@ -294,7 +294,7 @@ 
         return data
 
-    def _convert_and_standardize(self, data: pd.DataFrame) -> pd.DataFrame:
+async def _convert_and_standardize(self, data: pd.DataFrame) -> pd.DataFrame:
         """数据类型转换和标准化."""
         # 确保日期列格式正确
         date_columns = ["match_date", "created_at", "updated_at", "utc_date"]
@@ -319,7 +319,7 @@ 
         return data
 
-    def _advanced_validation(self, data: pd.DataFrame, data_type: str) -> pd.DataFrame:
+async def _advanced_validation(self, data: pd.DataFrame, data_type: str) -> pd.DataFrame:
         """高级数据验证."""
         validation_errors = []
 
@@ -372,7 +372,7 @@ 
         return data
 
-    def _generate_cleaning_report(
+async def _generate_cleaning_report(
         self, raw_data: pd.DataFrame, cleaned_data: pd.DataFrame, steps: list[str]
     ):
         """生成清洗报告."""
@@ -390,11 +390,11 @@ 
         logger.info(f"数据清洗报告生成完成: {self.data_quality_report}")
 
-    def get_cleaning_report(self) -> dict[str, Any]:
+async def get_cleaning_report(self) -> dict[str, Any]:
         """获取清洗报告."""
         return self.data_quality_report.copy()
 
-    def parse_match_json(self, raw_json: dict[str, Any]) -> dict[str, Any]:
+async def parse_match_json(self, raw_json: dict[str, Any]) -> dict[str, Any]:
         """解析比赛JSON数据为标准化格式.
 
         从API返回的JSON中提取关键信息并转换为符合Match模型的格式。
@@ -414,24 +414,24 @@ 
             # 提取基本信息
             match_data = {
-                "external_id": data.get("id"),
-                "status": data.get("status", "SCHEDULED"),
-                "match_date": self._parse_datetime(data.get("utcDate")),
-                "matchday": data.get("matchday"),
-                "season": data.get("season", {}).get("id")
-                if isinstance(data.get("season"), dict)
-                else data.get("season"),
+                "external_id": data.await get("id"),
+                "status": data.await get("status", "SCHEDULED"),
+                "match_date": self.await _parse_datetime(data.get("utcDate")),
+                "matchday": data.await get("matchday"),
+                "season": data.await await get("season", {}).get("id")
+                if await isinstance(data.get("season"), dict)
+                else data.await get("season"),
             }
 
             # 提取比分信息
-            score = data.get("score", {})
+            score = data.await get("score", {})
             if isinstance(score, dict):
-                full_time = score.get("fullTime", {})
+                full_time = score.await get("fullTime", {})
                 match_data.update(
                     {
-                        "home_score": full_time.get("home", 0),
-                        "away_score": full_time.get("away", 0),
-                        "winner": score.get("winner"),
+                        "home_score": full_time.await get("home", 0),
+                        "away_score": full_time.await get("away", 0),
+                        "winner": score.await get("winner"),
                     }
                 )
             else:
@@ -444,46 +444,46 @@                 )
 
             # 提取主队信息
-            home_team = data.get("homeTeam", {})
+            home_team = data.await get("homeTeam", {})
             if isinstance(home_team, dict):
                 match_data.update(
                     {
-                        "home_team_external_id": home_team.get("id"),
-                        "home_team_name": home_team.get("name"),
-                        "home_team_short_name": home_team.get("shortName"),
+                        "home_team_external_id": home_team.await get("id"),
+                        "home_team_name": home_team.await get("name"),
+                        "home_team_short_name": home_team.await get("shortName"),
                     }
                 )
 
             # 提取客队信息
-            away_team = data.get("awayTeam", {})
+            away_team = data.await get("awayTeam", {})
             if isinstance(away_team, dict):
                 match_data.update(
                     {
-                        "away_team_external_id": away_team.get("id"),
-                        "away_team_name": away_team.get("name"),
-                        "away_team_short_name": away_team.get("shortName"),
+                        "away_team_external_id": away_team.await get("id"),
+                        "away_team_name": away_team.await get("name"),
+                        "away_team_short_name": away_team.await get("shortName"),
                     }
                 )
 
             # 提取联赛信息
-            competition = data.get("competition", {})
+            competition = data.await get("competition", {})
             if isinstance(competition, dict):
                 match_data.update(
                     {
-                        "league_external_id": competition.get("id"),
-                        "league_name": competition.get("name"),
-                        "league_code": competition.get("code"),
+                        "league_external_id": competition.await get("id"),
+                        "league_name": competition.await get("name"),
+                        "league_code": competition.await get("code"),
                     }
                 )
 
             # 提取场地信息
-            match_data["venue"] = data.get("venue")
+            match_data["venue"] = data.await get("venue")
 
             # 验证必需字段
-            if not match_data.get("external_id"):
+            if not match_data.await get("external_id"):
                 raise ValueError("缺少external_id字段")
 
-            if not match_data.get("home_team_external_id") or not match_data.get(
+            if not match_data.await await get("home_team_external_id") or not match_data.get(
                 "away_team_external_id"
             ):
                 raise ValueError("缺少球队ID信息")
@@ -495,7 +495,7 @@             logger.error(f"JSON解析失败: {e}, 原始数据: {raw_json}")
             raise
 
-    def _parse_datetime(self, datetime_str: str) -> datetime:
+async def _parse_datetime(self, datetime_str: str) -> datetime:
         """解析ISO8601时间字符串为datetime对象.
 
         Args:
@@ -523,7 +523,7 @@             logger.warning(f"时间解析失败: {e}, 使用当前时间")
             return datetime.utcnow()
 
-    def extract_team_from_match(
+async def extract_team_from_match(
         self, match_data: dict[str, Any], team_type: str
     ) -> dict[str, Any]:
         """从比赛数据中提取球队信息.
@@ -537,22 +537,22 @@         """
         if team_type == "home":
             return {
-                "external_id": match_data.get("home_team_external_id"),
-                "name": match_data.get("home_team_name"),
-                "short_name": match_data.get("home_team_short_name"),
+                "external_id": match_data.await get("home_team_external_id"),
+                "name": match_data.await get("home_team_name"),
+                "short_name": match_data.await get("home_team_short_name"),
                 "country": "England",  # 基于当前英超数据的默认值
             }
         elif team_type == "away":
             return {
-                "external_id": match_data.get("away_team_external_id"),
-                "name": match_data.get("away_team_name"),
-                "short_name": match_data.get("away_team_short_name"),
+                "external_id": match_data.await get("away_team_external_id"),
+                "name": match_data.await get("away_team_name"),
+                "short_name": match_data.await get("away_team_short_name"),
                 "country": "England",  # 基于当前英超数据的默认值
             }
         else:
             raise ValueError("team_type必须是'home'或'away'")
 
-    def extract_league_from_match(self, match_data: dict[str, Any]) -> dict[str, Any]:
+async def extract_league_from_match(self, match_data: dict[str, Any]) -> dict[str, Any]:
         """从比赛数据中提取联赛信息.
 
         Args:
@@ -562,27 +562,27 @@             Dict: 联赛信息
         """
         return {
-            "external_id": match_data.get("league_external_id"),
-            "name": match_data.get("league_name", "Premier League"),
+            "external_id": match_data.await get("league_external_id"),
+            "name": match_data.await get("league_name", "Premier League"),
             "country": "England",  # 基于当前英超数据的默认值
             "is_active": True,
         }
 
-    def clean_match_data(self, raw_data: pd.DataFrame) -> pd.DataFrame:
+async def clean_match_data(self, raw_data: pd.DataFrame) -> pd.DataFrame:
         """清洗比赛数据的便捷方法."""
         return self.clean_dataset(raw_data, "matches")
 
-    def clean_team_data(self, raw_data: pd.DataFrame) -> pd.DataFrame:
+async def clean_team_data(self, raw_data: pd.DataFrame) -> pd.DataFrame:
         """清洗球队数据的便捷方法."""
         return self.clean_dataset(raw_data, "teams")
 
-    def clean_odds_data(self, raw_data: pd.DataFrame) -> pd.DataFrame:
+async def clean_odds_data(self, raw_data: pd.DataFrame) -> pd.DataFrame:
         """清洗赔率数据的便捷方法."""
         return self.clean_dataset(raw_data, "odds")
 
 
 # 便捷函数
-def clean_football_data(
+async def clean_football_data(
     raw_data: pd.DataFrame, data_type: str = "matches", config: dict | None = None
 ) -> pd.DataFrame:
     """便捷的足球数据清洗函数.
