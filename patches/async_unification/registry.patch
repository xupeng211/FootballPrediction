--- a/src/adapters/registry.py+++ b/src/adapters/registry.py@@ -13,7 +13,7 @@     from .factory_simple import get_global_factory
 except ImportError:
 
-    def get_global_factory():
+async def get_global_factory():
         return None
 
 
@@ -26,20 +26,20 @@ class BaseAdapterRegistry:
     """适配器注册表."""
 
-    def __init__(self, factory=None):
+async def __init__(self, factory=None):
         """初始化适配器注册表."""
         self.factory = factory or self._create_default_factory()
         self.adapters: dict[str, BaseAdapter] = {}
         self.groups: dict[str, list[BaseAdapter]] = {}
 
-    def _create_default_factory(self):
+async def _create_default_factory(self):
         """创建默认工厂."""
         try:
             return get_global_factory()
         except ImportError:
             return None
 
-    def register(
+async def register(
         self, name: str, adapter: BaseAdapter, group: str | None = None
     ) -> None:
         """注册适配器."""
@@ -49,22 +49,22 @@                 self.groups[group] = []
             self.groups[group].append(adapter)
 
-    def get_adapter(self, name: str) -> BaseAdapter:
+async def get_adapter(self, name: str) -> BaseAdapter:
         """获取适配器."""
         if name not in self.adapters:
             raise BaseAdapterError(f"BaseAdapter '{name}' not found")
         return self.adapters[name]
 
-    def get_group(self, group: str) -> list[BaseAdapter]:
+async def get_group(self, group: str) -> list[BaseAdapter]:
         """获取适配器组."""
-        return self.groups.get(group, [])
+        return self.groups.await get(group, [])
 
 
 # 全局注册表实例
 _global_registry = BaseAdapterRegistry()
 
 
-def get_global_registry() -> BaseAdapterRegistry:
+async def get_global_registry() -> BaseAdapterRegistry:
     """获取全局注册表."""
     return _global_registry
 
