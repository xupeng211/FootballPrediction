--- a/src/cache/decorators.py+++ b/src/cache/decorators.py@@ -8,12 +8,12 @@ logger = logging.getLogger(__name__)
 
 
-def cache_result(ttl: int = 300, key_func: Callable | None = None):
+async def cache_result(ttl: int = 300, key_func: Callable | None = None):
     """缓存结果装饰器."""
 
-    def decorator(func: Callable) -> Callable:
+async def decorator(func: Callable) -> Callable:
         @functools.wraps(func)
-        def sync_wrapper(*args, **kwargs):
+async def sync_wrapper(*args, **kwargs):
             # 简化的缓存逻辑
             return func(*args, **kwargs)
 
@@ -27,12 +27,12 @@     return decorator
 
 
-def invalidate_cache(pattern: str = "*"):
+async def invalidate_cache(pattern: str = "*"):
     """缓存失效装饰器."""
 
-    def decorator(func: Callable) -> Callable:
+async def decorator(func: Callable) -> Callable:
         @functools.wraps(func)
-        def wrapper(*args, **kwargs):
+async def wrapper(*args, **kwargs):
             try:
                 result = func(*args, **kwargs)
                 logger.debug(f"缓存失效: pattern={pattern}")
@@ -50,27 +50,27 @@ class CacheDecorator:
     """缓存装饰器基类."""
 
-    def __init__(self, ttl: int = 300):
+async def __init__(self, ttl: int = 300):
         self.ttl = ttl
 
-    def __call__(self, func: Callable) -> Callable:
+async def __call__(self, func: Callable) -> Callable:
         return cache_result(self.ttl)(func)
 
 
 class InvalidateCacheDecorator:
     """缓存失效装饰器."""
 
-    def __init__(self, pattern: str = "*"):
+async def __init__(self, pattern: str = "*"):
         self.pattern = pattern
 
-    def __call__(self, func: Callable) -> Callable:
+async def __call__(self, func: Callable) -> Callable:
         return invalidate_cache(self.pattern)(func)
 
 
 class UserCacheDecorator(CacheDecorator):
     """用户缓存装饰器."""
 
-    def __init__(self, ttl: int = 300):
+async def __init__(self, ttl: int = 300):
         super().__init__(ttl)
 
 
