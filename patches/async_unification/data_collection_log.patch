--- a/src/database/models/data_collection_log.py+++ b/src/database/models/data_collection_log.py@@ -73,7 +73,7 @@     )
 
     @validates("collection_type")
-    def validate_collection_type(self, key: str, collection_type: str) -> str:
+async def validate_collection_type(self, key: str, collection_type: str) -> str:
         """验证采集类型."""
         valid_types = [ct.value for ct in CollectionType]
         if collection_type not in valid_types:
@@ -83,7 +83,7 @@         return collection_type
 
     @validates("status")
-    def validate_status(self, key: str, status: str) -> str:
+async def validate_status(self, key: str, status: str) -> str:
         """验证采集状态."""
         valid_statuses = [cs.value for cs in CollectionStatus]
         if status not in valid_statuses:
@@ -93,14 +93,14 @@         return status
 
     @property
-    def duration_seconds(self) -> float | None:
+async def duration_seconds(self) -> float | None:
         """计算采集耗时（秒）."""
         if self.start_time and self.end_time:
             return (self.end_time - self.start_time).total_seconds()
         return None
 
     @property
-    def success_rate(self) -> float:
+async def success_rate(self) -> float:
         """计算成功率."""
         total = self.success_count + self.error_count
         if total == 0:
@@ -108,7 +108,7 @@         return float(self.success_count) / float(total)
 
     @property
-    def is_completed(self) -> bool:
+async def is_completed(self) -> bool:
         """是否已完成（成功或失败）."""
         return self.status in (
             CollectionStatus.SUCCESS.value,
@@ -117,16 +117,16 @@         )
 
     @property
-    def is_successful(self) -> bool:
+async def is_successful(self) -> bool:
         """是否成功完成."""
         return self.status == CollectionStatus.SUCCESS.value
 
-    def mark_started(self) -> None:
+async def mark_started(self) -> None:
         """标记采集开始."""
         self.start_time = datetime.now()
         self.status = CollectionStatus.RUNNING.value
 
-    def mark_completed(
+async def mark_completed(
         self,
         status: CollectionStatus,
         records_collected: int = 0,
@@ -151,7 +151,7 @@         if error_message:
             self.error_message = error_message
 
-    def __repr__(self) -> str:
+async def __repr__(self) -> str:
         """字符串表示."""
         return (
             f"<DataCollectionLog("
@@ -163,7 +163,7 @@             f")>"
         )
 
-    def to_dict(self, exclude_fields: set | None = None) -> dict[str, Any]:
+async def to_dict(self, exclude_fields: set | None = None) -> dict[str, Any]:
         """转换为字典格式."""
         return {
             "id": self.id,
