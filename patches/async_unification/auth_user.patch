--- a/src/models/auth_user.py+++ b/src/models/auth_user.py@@ -77,21 +77,21 @@         Index("idx_users_last_login", "last_login_at"),
     )
 
-    def __repr__(self):
+async def __repr__(self):
         return f"<User(id={self.id}, username='{self.username}', email='{self.email}')>"
 
     @property
-    def is_admin(self) -> bool:
+async def is_admin(self) -> bool:
         """是否为管理员."""
         return self.role == "admin"
 
     @property
-    def is_manager(self) -> bool:
+async def is_manager(self) -> bool:
         """是否为管理员或经理."""
         return self.role in ["admin", "manager"]
 
     @property
-    def is_locked(self) -> bool:
+async def is_locked(self) -> bool:
         """是否被锁定."""
         if not self.locked_until:
             return False
@@ -99,7 +99,7 @@ 
         return datetime.utcnow() < self.locked_until
 
-    def to_dict(self) -> dict:
+async def to_dict(self) -> dict:
         """转换为字典."""
         return {
             "id": self.id,
@@ -163,7 +163,7 @@         Index("idx_user_sessions_expires", "expires_at"),
     )
 
-    def __repr__(self):
+async def __repr__(self):
         return f"<UserSession(id={self.id}, user_id={self.user_id}, active={self.is_active})>"
 
 
@@ -201,7 +201,7 @@         Index("idx_audit_logs_ip", "ip_address"),
     )
 
-    def __repr__(self):
+async def __repr__(self):
         return (
             f"<AuditLog(id={self.id}, user_id={self.user_id}, action='{self.action}')>"
         )
