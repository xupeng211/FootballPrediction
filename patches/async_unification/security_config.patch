--- a/src/config/security_config.py+++ b/src/config/security_config.py@@ -19,11 +19,11 @@ class SecurityConfig:
     """安全配置管理类."""
 
-    def __init__(self):
+async def __init__(self):
         """初始化安全配置."""
         self._load_config()
 
-    def _load_config(self):
+async def _load_config(self):
         """加载安全配置."""
         # 数据库配置
         self.database_url = os.getenv("DATABASE_URL")
@@ -63,7 +63,7 @@         self.test_user_email = os.getenv("TEST_USER_EMAIL", "test@example.com")
         self.test_user_password_hash = os.getenv("TEST_USER_PASSWORD_HASH")
 
-    def get_database_url(self) -> str:
+async def get_database_url(self) -> str:
         """获取数据库连接URL."""
         if self.database_url:
             return self.database_url
@@ -73,7 +73,7 @@ 
         return f"postgresql://{self.database_user}:{self.database_password}@{self.database_host}:{self.database_port}/{self.database_name}"
 
-    def get_redis_url(self) -> str:
+async def get_redis_url(self) -> str:
         """获取Redis连接URL."""
         if self.redis_url:
             return self.redis_url
@@ -85,7 +85,7 @@         else:
             return f"redis://{self.redis_host}:{self.redis_port}/0"
 
-    def validate(self) -> dict[str, Any]:
+async def validate(self) -> dict[str, Any]:
         """验证配置完整性."""
         issues = []
         warnings = []
@@ -120,7 +120,7 @@             "status": "OK" if len(issues) == 0 else "NEEDS_ATTENTION",
         }
 
-    def get_safe_config(self) -> dict[str, Any]:
+async def get_safe_config(self) -> dict[str, Any]:
         """获取安全的配置信息（隐藏敏感信息）."""
         return {
             "database_host": self.database_host,
@@ -140,23 +140,23 @@ security_config = SecurityConfig()
 
 
-def get_security_config() -> SecurityConfig:
+async def get_security_config() -> SecurityConfig:
     """获取全局安全配置实例."""
     return security_config
 
 
 # 便捷函数
-def validate_security_config() -> dict[str, Any]:
+async def validate_security_config() -> dict[str, Any]:
     """验证安全配置的便捷函数."""
     return security_config.validate()
 
 
-def get_database_url() -> str:
+async def get_database_url() -> str:
     """获取数据库URL的便捷函数."""
     return security_config.get_database_url()
 
 
-def get_redis_url() -> str:
+async def get_redis_url() -> str:
     """获取Redis URL的便捷函数."""
     return security_config.get_redis_url()
 
