--- a/src/database/definitions.py+++ b/src/database/definitions.py@@ -24,18 +24,18 @@     _session_factory = None
     _async_session_factory = None
 
-    def __new__(cls):
+async def __new__(cls):
         """函数文档字符串."""
         if cls._instance is None:
             cls._instance = super().__new__(cls)
         return cls._instance
 
-    def __init__(self):
+async def __init__(self):
         """函数文档字符串."""
         if not hasattr(self, "initialized"):
             self.initialized = False
 
-    def initialize(self, database_url: str | None = None):
+async def initialize(self, database_url: str | None = None):
         """初始化数据库连接."""
         if self.initialized:
             return None
@@ -69,7 +69,7 @@ 
         self.initialized = True
 
-    def get_session(self) -> Session:
+async def get_session(self) -> Session:
         """获取同步会话."""
         if not self.initialized or not self._session_factory:
             raise RuntimeError(
@@ -77,7 +77,7 @@             )
         return self._session_factory()
 
-    def get_async_session(self) -> AsyncSession:
+async def get_async_session(self) -> AsyncSession:
         """获取异步会话."""
         if not self.initialized or not self._async_session_factory:
             raise RuntimeError(
@@ -85,7 +85,7 @@             )
         return self._async_session_factory()
 
-    def get_sync_connection(self):
+async def get_sync_connection(self):
         """获取同步数据库连接."""
         if not self.initialized or not self._engine:
             raise RuntimeError(
@@ -97,7 +97,7 @@ class MultiUserDatabaseManager(DatabaseManager):
     """多用户数据库管理器 - 简化版本."""
 
-    def __init__(self):
+async def __init__(self):
         """函数文档字符串."""
         super().__init__()
         self.readers = []
@@ -106,69 +106,69 @@ 
 
 # 工厂函数
-def get_database_manager() -> DatabaseManager:
+async def get_database_manager() -> DatabaseManager:
     """获取数据库管理器单例."""
     return DatabaseManager()
 
 
-def get_multi_user_database_manager() -> MultiUserDatabaseManager:
+async def get_multi_user_database_manager() -> MultiUserDatabaseManager:
     """获取多用户数据库管理器."""
     return MultiUserDatabaseManager()
 
 
 # 初始化函数
-def initialize_database(database_url: str | None = None):
+async def initialize_database(database_url: str | None = None):
     """初始化数据库."""
     manager = get_database_manager()
     manager.initialize(database_url)
 
 
-def initialize_multi_user_database(database_url: str | None = None):
+async def initialize_multi_user_database(database_url: str | None = None):
     """初始化多用户数据库."""
     manager = get_multi_user_database_manager()
     manager.initialize(database_url)
 
 
-def initialize_test_database():
+async def initialize_test_database():
     """初始化测试数据库."""
     # 测试数据库的特殊初始化
 
 
 # 会话获取函数
-def get_db_session() -> Session:
+async def get_db_session() -> Session:
     """获取数据库会话."""
     manager = get_database_manager()
     return manager.get_session()
 
 
-def get_async_session() -> AsyncSession:
+async def get_async_session() -> AsyncSession:
     """获取异步会话."""
     manager = get_database_manager()
-    return manager.get_async_session()
+    return manager.await get_async_session()
 
 
 # 其他会话函数的简化实现
-def get_reader_session() -> Session:
-    return get_db_session()
+async def get_reader_session() -> Session:
+    return await get_db_session()
 
 
-def get_writer_session() -> Session:
-    return get_db_session()
+async def get_writer_session() -> Session:
+    return await get_db_session()
 
 
-def get_admin_session() -> Session:
-    return get_db_session()
+async def get_admin_session() -> Session:
+    return await get_db_session()
 
 
-def get_async_reader_session() -> AsyncSession:
+async def get_async_reader_session() -> AsyncSession:
     return get_async_session()
 
 
-def get_async_writer_session() -> AsyncSession:
+async def get_async_writer_session() -> AsyncSession:
     return get_async_session()
 
 
-def get_async_admin_session() -> AsyncSession:
+async def get_async_admin_session() -> AsyncSession:
     return get_async_session()
 
 
