--- a/src/collectors/http_client_factory.py+++ b/src/collectors/http_client_factory.py@@ -41,27 +41,27 @@     """采集器配置协议"""
 
     @property
-    def source_name(self) -> str:
+async def source_name(self) -> str:
         """数据源名称"""
         ...
 
     @property
-    def base_url(self) -> str:
+async def base_url(self) -> str:
         """基础URL"""
         ...
 
     @property
-    def rate_limit_config(self) -> Dict[str, Any]:
+async def rate_limit_config(self) -> Dict[str, Any]:
         """速率限制配置"""
         ...
 
     @property
-    def token_manager_config(self) -> Dict[str, Any]:
+async def token_manager_config(self) -> Dict[str, Any]:
         """Token管理器配置"""
         ...
 
     @property
-    def proxy_config(self) -> Optional[Dict[str, Any]]:
+async def proxy_config(self) -> Optional[Dict[str, Any]]:
         """代理配置"""
         ...
 
@@ -110,7 +110,7 @@ class RequestEvent:
     """请求事件数据类"""
 
-    def __init__(
+async def __init__(
         self,
         source: str,
         method: str,
@@ -131,7 +131,7 @@         self.token_refreshed = token_refreshed
         self.timestamp = time.monotonic()
 
-    def to_dict(self) -> Dict[str, Any]:
+async def to_dict(self) -> Dict[str, Any]:
         """转换为字典格式"""
         return {
             "source": self.source,
@@ -149,7 +149,7 @@ class RequestMonitor:
     """请求监控器"""
 
-    def __init__(self):
+async def __init__(self):
         self.events: List[RequestEvent] = []
         self.stats: Dict[str, Any] = {
             "total_requests": 0,
@@ -160,7 +160,7 @@             "proxy_rotations": 0,
         }
 
-    def record_event(self, event: RequestEvent) -> None:
+async def record_event(self, event: RequestEvent) -> None:
         """记录请求事件"""
         self.events.append(event)
 
@@ -181,7 +181,7 @@         if event.proxy_used:
             self.stats["proxy_rotations"] += 1
 
-    def get_stats(self) -> Dict[str, Any]:
+async def get_stats(self) -> Dict[str, Any]:
         """获取统计信息"""
         total_requests = self.stats["total_requests"]
         avg_response_time = (
@@ -202,7 +202,7 @@             ),
         }
 
-    def get_events(self, source: Optional[str] = None, limit: Optional[int] = None) -> List[RequestEvent]:
+async def get_events(self, source: Optional[str] = None, limit: Optional[int] = None) -> List[RequestEvent]:
         """获取事件列表"""
         events = self.events
         if source:
@@ -211,7 +211,7 @@             events = events[-limit:]
         return events
 
-    def clear(self) -> None:
+async def clear(self) -> None:
         """清除所有事件和统计"""
         self.events.clear()
         self.stats = {
@@ -235,7 +235,7 @@     4. 可测试的依赖注入支持
     """
 
-    def __init__(self):
+async def __init__(self):
         self._components: Dict[str, Any] = {}
         self._monitor = RequestMonitor()
 
@@ -244,15 +244,15 @@             "fotmob": FotMobConfig(),
         }
 
-    def register_config(self, source: str, config: CollectorConfig) -> None:
+async def register_config(self, source: str, config: CollectorConfig) -> None:
         """注册数据源配置"""
         self._configs[source] = config
 
-    def register_component(self, name: str, component: Any) -> None:
+async def register_component(self, name: str, component: Any) -> None:
         """注册组件（用于依赖注入）"""
         self._components[name] = component
 
-    def get_monitor(self) -> RequestMonitor:
+async def get_monitor(self) -> RequestMonitor:
         """获取请求监控器"""
         return self._monitor
 
@@ -416,19 +416,19 @@ 
         return httpx.AsyncClient(**client_config)
 
-    def get_available_sources(self) -> List[str]:
+async def get_available_sources(self) -> List[str]:
         """获取可用的数据源列表"""
         return list(self._configs.keys())
 
-    def get_config(self, source: str) -> Optional[CollectorConfig]:
+async def get_config(self, source: str) -> Optional[CollectorConfig]:
         """获取数据源配置"""
-        return self._configs.get(source)
+        return self._configs.await get(source)
 
 
 class MonitoredCollector:
     """带监控功能的采集器包装器"""
 
-    def __init__(
+async def __init__(
         self,
         collector: BaseCollectorProtocol,
         source: str,
@@ -558,7 +558,7 @@         """关闭采集器"""
         await self.collector.close()
 
-    def __getattr__(self, name):
+async def __getattr__(self, name):
         """转发其他属性调用到原始采集器"""
         return getattr(self.collector, name)
 
@@ -567,7 +567,7 @@ _global_factory: Optional[HttpClientFactory] = None
 
 
-def get_http_client_factory() -> HttpClientFactory:
+async def get_http_client_factory() -> HttpClientFactory:
     """获取全局HTTP客户端工厂实例"""
     global _global_factory
     if _global_factory is None:
