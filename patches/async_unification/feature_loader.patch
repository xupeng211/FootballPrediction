--- a/src/pipeline/feature_loader.py+++ b/src/pipeline/feature_loader.py@@ -52,7 +52,7 @@     - 批量数据加载
     """
 
-    def __init__(
+async def __init__(
         self,
         store: FeatureStoreProtocol,
         config: Optional[PipelineConfig] = None,
@@ -82,7 +82,7 @@         # 缓存
         self._feature_cache: Dict[str, pd.DataFrame] = {}
 
-    def load_training_data(
+async def load_training_data(
         self,
         match_ids: List[int],
         target_column: str = "result",
@@ -188,7 +188,7 @@         logger.info(f"Successfully loaded {len(feature_data_list)} feature records")
         return feature_data_list
 
-    def _convert_to_dataframe(
+async def _convert_to_dataframe(
         self, feature_data_list: List[FeatureData], match_ids: List[int]
     ) -> pd.DataFrame:
         """将特征数据转换为DataFrame."""
@@ -221,7 +221,7 @@ 
         return df
 
-    def _preprocess_features(self, df: pd.DataFrame) -> pd.DataFrame:
+async def _preprocess_features(self, df: pd.DataFrame) -> pd.DataFrame:
         """特征工程和预处理."""
         logger.debug("Preprocessing features")
 
@@ -241,7 +241,7 @@         logger.debug(f"Preprocessed {len(df_processed)} samples")
         return df_processed
 
-    def _handle_missing_values(self, df: pd.DataFrame) -> pd.DataFrame:
+async def _handle_missing_values(self, df: pd.DataFrame) -> pd.DataFrame:
         """处理缺失值."""
         numeric_columns = df.select_dtypes(include=[np.number]).columns
 
@@ -258,7 +258,7 @@ 
         return df
 
-    def _normalize_features(self, df: pd.DataFrame) -> pd.DataFrame:
+async def _normalize_features(self, df: pd.DataFrame) -> pd.DataFrame:
         """特征标准化."""
         numeric_columns = df.select_dtypes(include=[np.number]).columns
         numeric_columns = numeric_columns.drop("match_id", errors="ignore")
@@ -272,7 +272,7 @@ 
         return df
 
-    def _encode_categorical(self, df: pd.DataFrame) -> pd.DataFrame:
+async def _encode_categorical(self, df: pd.DataFrame) -> pd.DataFrame:
         """分类变量编码."""
         categorical_columns = df.select_dtypes(include=["object", "category"]).columns
 
@@ -319,7 +319,7 @@ 
         return quality_result
 
-    def _check_numeric_ranges(self, df: pd.DataFrame) -> Dict[str, Any]:
+async def _check_numeric_ranges(self, df: pd.DataFrame) -> Dict[str, Any]:
         """检查数值范围."""
         numeric_cols = df.select_dtypes(include=[np.number]).columns
         range_info = {}
@@ -339,7 +339,7 @@ 
         return range_info
 
-    def get_feature_stats(self, df: pd.DataFrame) -> Dict[str, Any]:
+async def get_feature_stats(self, df: pd.DataFrame) -> Dict[str, Any]:
         """获取特征统计信息."""
         stats = {
             "shape": df.shape,
@@ -350,7 +350,7 @@ 
         return stats
 
-    def save_preprocessors(self, path: str) -> None:
+async def save_preprocessors(self, path: str) -> None:
         """保存预处理器（用于推理时复用）."""
         import joblib
         from pathlib import Path
@@ -364,7 +364,7 @@ 
         logger.info(f"Preprocessors saved to {save_path}")
 
-    def load_preprocessors(self, path: str) -> None:
+async def load_preprocessors(self, path: str) -> None:
         """加载预处理器."""
         import joblib
         from pathlib import Path
@@ -379,7 +379,7 @@ 
 
 # 便捷函数
-def create_feature_loader(
+async def create_feature_loader(
     store: FeatureStoreProtocol, config: Optional[PipelineConfig] = None
 ) -> FeatureLoader:
     """创建特征加载器的便捷函数."""
