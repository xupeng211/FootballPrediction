--- a/src/services/real_data.py+++ b/src/services/real_data.py@@ -21,7 +21,7 @@ class RealDataService:
     """真实数据服务类 - 基于SQLAlchemy的数据库查询."""
 
-    def __init__(self, session: AsyncSession):
+async def __init__(self, session: AsyncSession):
         """
         初始化真实数据服务.
 
@@ -50,12 +50,12 @@ 
             # 查询球队总数
             count_query = select(Team)
-            count_result = await self.session.execute(count_query)
+            count_result = await self.session.await execute(count_query)
             total_teams = len(count_result.scalars().all())
 
             # 分页查询球队数据
             query = select(Team).offset(offset).limit(limit)
-            result = await self.session.execute(query)
+            result = await self.session.await execute(query)
             teams = result.scalars().all()
 
             # 转换为API响应格式
@@ -108,7 +108,7 @@             self.logger.info(f"Querying team {team_id} from database")
 
             query = select(Team).where(Team.id == team_id)
-            result = await self.session.execute(query)
+            result = await self.session.await execute(query)
             team = result.scalar_one_or_none()
 
             if not team:
@@ -155,7 +155,7 @@ 
             # 查询比赛总数
             count_query = select(Match)
-            count_result = await self.session.execute(count_query)
+            count_result = await self.session.await execute(count_query)
             total_matches = len(count_result.scalars().all())
 
             # 关联查询比赛、主队、客队和联赛
@@ -168,7 +168,7 @@                 .offset(offset)
                 .limit(limit)
             )
-            result = await self.session.execute(query)
+            result = await self.session.await execute(query)
             matches = result.scalars().all()
 
             # 转换为API响应格式
@@ -233,7 +233,7 @@                 )
                 .where(Match.id == match_id)
             )
-            result = await self.session.execute(query)
+            result = await self.session.await execute(query)
             match = result.scalar_one_or_none()
 
             if not match:
@@ -269,7 +269,7 @@             raise e
 
 
-def get_real_data_service(session: AsyncSession) -> RealDataService:
+async def get_real_data_service(session: AsyncSession) -> RealDataService:
     """
     获取真实数据服务实例
     Get real data service instance.
