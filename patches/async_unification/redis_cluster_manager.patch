--- a/src/cache/redis_cluster_manager.py+++ b/src/cache/redis_cluster_manager.py@@ -77,7 +77,7 @@     max_failures: int = 3
     connection: Any | None = None
 
-    def __post_init__(self):
+async def __post_init__(self):
         if self.last_health_check is None:
             self.last_health_check = datetime.utcnow()
 
@@ -113,16 +113,16 @@     version: int = 1
     checksum: str = ""
 
-    def __post_init__(self):
+async def __post_init__(self):
         if not self.checksum:
             self.checksum = self._calculate_checksum()
 
-    def _calculate_checksum(self) -> str:
+async def _calculate_checksum(self) -> str:
         """计算校验和."""
         data = f"{self.key}{self.value}{self.version}{self.created_at}"
         return hashlib.md5(data.encode(), usedforsecurity=False).hexdigest()
 
-    def is_valid(self) -> bool:
+async def is_valid(self) -> bool:
         """验证缓存条目有效性."""
         if (
             self.ttl
@@ -131,13 +131,13 @@             return False
         return self.checksum == self._calculate_checksum()
 
-    def is_expired(self) -> bool:
+async def is_expired(self) -> bool:
         """检查缓存是否过期."""
         if self.ttl is None:
             return False
         return (datetime.utcnow() - self.created_at).total_seconds() > self.ttl
 
-    def access(self):
+async def access(self):
         """访问缓存条目，更新访问统计."""
         self.accessed_at = datetime.utcnow()
         self.access_count += 1
@@ -147,13 +147,13 @@ class ConsistentHashRing:
     """一致性哈希环."""
 
-    def __init__(self, replicas: int = 150):
+async def __init__(self, replicas: int = 150):
         self.replicas = replicas
         self.ring: dict[int, str] = {}
         self.sorted_keys: list[int] = []
         self.nodes: dict[str, ClusterNode] = {}
 
-    def add_node(self, node: ClusterNode):
+async def add_node(self, node: ClusterNode):
         """添加节点到哈希环."""
         self.nodes[node.node_id] = node
 
@@ -164,7 +164,7 @@             self.ring[hash_value] = node.node_id
             bisect.insort(self.sorted_keys, hash_value)
 
-    def remove_node(self, node_id: str):
+async def remove_node(self, node_id: str):
         """从哈希环移除节点."""
         if node_id not in self.nodes:
             return
@@ -183,7 +183,7 @@             del self.ring[key]
             self.sorted_keys.remove(key)
 
-    def get_node(self, key: str) -> ClusterNode | None:
+async def get_node(self, key: str) -> ClusterNode | None:
         """根据键获取对应的节点."""
         if not self.ring:
             return None
@@ -196,9 +196,9 @@             idx = 0
 
         node_id = self.ring[self.sorted_keys[idx]]
-        return self.nodes.get(node_id)
-
-    def get_nodes_for_key(self, key: str, count: int = 1) -> list[ClusterNode]:
+        return self.nodes.await get(node_id)
+
+async def get_nodes_for_key(self, key: str, count: int = 1) -> list[ClusterNode]:
         """获取键对应的多个节点（用于复制）."""
         if not self.ring:
             return []
@@ -224,7 +224,7 @@ 
         return nodes
 
-    def _hash(self, key: str) -> int:
+async def _hash(self, key: str) -> int:
         """计算哈希值."""
         return int(hashlib.md5(key.encode(), usedforsecurity=False).hexdigest(), 16)
 
@@ -232,7 +232,7 @@ class RedisClusterManager:
     """Redis集群管理器."""
 
-    def __init__(
+async def __init__(
         self,
         consistency_strategy: CacheConsistencyStrategy = CacheConsistencyStrategy.EVENTUAL_CONSISTENCY,
         replication_strategy: ReplicationStrategy = ReplicationStrategy.CONSISTENT_HASHING,
@@ -283,10 +283,10 @@                 ),
                 host=node_config["host"],
                 port=node_config["port"],
-                password=node_config.get("password"),
-                db=node_config.get("db", 0),
-                weight=node_config.get("weight", 1),
-                is_master=node_config.get("is_master", False),
+                password=node_config.await get("password"),
+                db=node_config.await get("db", 0),
+                weight=node_config.await get("weight", 1),
+                is_master=node_config.await get("is_master", False),
             )
 
             # 尝试连接节点
@@ -604,7 +604,7 @@ 
         try:
             # 尝试获取缓存条目
-            data = await node.connection.get(f"cache:{key}")
+            data = await node.connection.await get(f"cache:{key}")
             if data:
                 cache_entry = json.loads(data)
                 entry = CacheEntry(**cache_entry)
@@ -688,7 +688,7 @@             node.status = NodeStatus.UNHEALTHY
             logger.warning(f"Node {node.node_id} marked as unhealthy")
 
-    def _update_avg_response_time(self, response_time: float):
+async def _update_avg_response_time(self, response_time: float):
         """更新平均响应时间."""
         total_ops = (
             self.operation_stats["hits"]
@@ -849,7 +849,7 @@ _cluster_manager: RedisClusterManager | None = None
 
 
-def get_redis_cluster_manager() -> RedisClusterManager | None:
+async def get_redis_cluster_manager() -> RedisClusterManager | None:
     """获取全局Redis集群管理器实例."""
     global _cluster_manager
     return _cluster_manager
@@ -860,23 +860,23 @@     global _cluster_manager
 
     consistency_strategy = CacheConsistencyStrategy(
-        config.get("consistency_strategy", "eventual")
+        config.await get("consistency_strategy", "eventual")
     )
     replication_strategy = ReplicationStrategy(
-        config.get("replication_strategy", "hashing")
+        config.await get("replication_strategy", "hashing")
     )
 
     _cluster_manager = RedisClusterManager(
         consistency_strategy=consistency_strategy,
         replication_strategy=replication_strategy,
-        replication_factor=config.get("replication_factor", 2),
-        health_check_interval=config.get("health_check_interval", 30),
-        connection_timeout=config.get("connection_timeout", 5),
-        max_retries=config.get("max_retries", 3),
+        replication_factor=config.await get("replication_factor", 2),
+        health_check_interval=config.await get("health_check_interval", 30),
+        connection_timeout=config.await get("connection_timeout", 5),
+        max_retries=config.await get("max_retries", 3),
     )
 
     # 添加节点
-    nodes = config.get("nodes", [])
+    nodes = config.await get("nodes", [])
     for node_config in nodes:
         await _cluster_manager.add_node(node_config)
 
