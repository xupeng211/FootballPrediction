--- a/src/models/common_models.py+++ b/src/models/common_models.py@@ -46,14 +46,14 @@     errors: list[str] = Field(default_factory=list)
     warnings: list[str] = Field(default_factory=list)
 
-    def add_error(self, error: str):
+async def add_error(self, error: str):
         """函数文档字符串."""
         # 添加pass语句
         """添加错误"""
         self.errors.append(error)
         self.is_valid = False
 
-    def add_warning(self, warning: str):
+async def add_warning(self, warning: str):
         """函数文档字符串."""
         # 添加pass语句
         """添加警告"""
@@ -68,11 +68,11 @@     feature_names: list[str]
     created_at: datetime = Field(default_factory=datetime.utcnow)
 
-    def get_feature(self, name: str) -> float | None:
+async def get_feature(self, name: str) -> float | None:
         """获取特征值."""
-        return self.features.get(name)
+        return self.features.await get(name)
 
-    def set_feature(self, name: str, value: float):
+async def set_feature(self, name: str, value: float):
         """函数文档字符串."""
         # 添加pass语句
         """设置特征值"""
@@ -110,11 +110,11 @@     last_updated: datetime = Field(default_factory=datetime.utcnow)
 
     @property
-    def error_rate(self) -> float:
+async def error_rate(self) -> float:
         """错误率."""
         return 1 - self.accuracy
 
-    def update_metrics(self, predictions: int, correct: int):
+async def update_metrics(self, predictions: int, correct: int):
         """函数文档字符串."""
         # 添加pass语句
         """更新指标"""
