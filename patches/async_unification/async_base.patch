--- a/src/core/async_base.py+++ b/src/core/async_base.py@@ -52,14 +52,14 @@     last_request_time: Optional[datetime] = None
 
     @property
-    def success_rate(self) -> float:
+async def success_rate(self) -> float:
         """成功率"""
         if self.total_requests == 0:
             return 0.0
         return (self.successful_requests / self.total_requests) * 100
 
     @property
-    def average_response_time_ms(self) -> float:
+async def average_response_time_ms(self) -> float:
         """平均响应时间"""
         if self.total_requests == 0:
             return 0.0
@@ -74,7 +74,7 @@     所有数据采集器都应继承此基类
     """
 
-    def __init__(
+async def __init__(
         self,
         config: Optional[AsyncConfig] = None,
         name: Optional[str] = None
@@ -243,7 +243,7 @@                     await asyncio.sleep(delay)
                     self.logger.debug(f"Retry attempt {attempt} for {url}")
 
-                response = await self.fetch(url, method, **kwargs)
+                response = await self.await fetch(url, method, **kwargs)
 
                 # 如果是客户端错误(4xx)，不重试
                 if 400 <= response.status_code < 500:
@@ -291,7 +291,7 @@         response = await self.fetch_with_retry(url, method, **kwargs)
         return response.json()
 
-    def get_stats(self) -> Dict[str, Any]:
+async def get_stats(self) -> Dict[str, Any]:
         """
         获取采集器统计信息
 
@@ -310,7 +310,7 @@             "is_initialized": self._is_initialized,
         }
 
-    def reset_stats(self):
+async def reset_stats(self):
         """重置统计信息"""
         self.stats = RequestStats()
         self.logger.info(f"Stats reset for {self.name}")
@@ -323,7 +323,7 @@     提供数据库会话管理、日志记录等通用服务功能
     """
 
-    def __init__(self, name: Optional[str] = None):
+async def __init__(self, name: Optional[str] = None):
         self.name = name or self.__class__.__name__
         self.logger = get_logger(f"{self.__class__.__module__}.{self.name}")
 
@@ -335,7 +335,7 @@         Returns:
             AsyncGenerator[AsyncSession, None]: 数据库会话
         """
-        async with get_db_session() as session:
+        async with await get_db_session() as session:
             try:
                 yield session
                 await session.commit()
@@ -361,7 +361,7 @@             查询结果
         """
         async with self.get_db_session() as session:
-            result = await session.execute(query, params)
+            result = await session.await execute(query, params)
             return result
 
     async def log_performance(
@@ -392,7 +392,7 @@     用于批量处理数据采集任务
     """
 
-    def __init__(self, batch_size: int = 10, name: Optional[str] = None):
+async def __init__(self, batch_size: int = 10, name: Optional[str] = None):
         super().__init__(name)
         self.batch_size = batch_size
 
