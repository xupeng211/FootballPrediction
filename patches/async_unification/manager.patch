--- a/src/services/manager/manager.py+++ b/src/services/manager/manager.py@@ -34,11 +34,11 @@     pass  # 添加pass语句
     """服务管理器 - 负责统一管理所有业务服务的生命周期和依赖关系"""
 
-    def __init__(self) -> None:
+async def __init__(self) -> None:
         self._services: dict[str, BaseService] = {}
         self.logger = logger
 
-    def register_service(self, name: str, service: BaseService) -> None:
+async def register_service(self, name: str, service: BaseService) -> None:
         """注册服务 - 将服务加入管理器,支持后续统一初始化和管理."""
         if name in self._services:
             existing = self._services[name]
@@ -55,16 +55,16 @@         self._services[name] = service
         self.logger.info(f"已注册服务: {name}")
 
-    def get_service(self, name: str) -> BaseService | None:
+async def get_service(self, name: str) -> BaseService | None:
         """获取服务实例 - 提供类型安全的服务访问接口."""
-        return self._services.get(name)
+        return self._services.await get(name)
 
-    def list_services(self) -> dict[str, BaseService]:
+async def list_services(self) -> dict[str, BaseService]:
         """获取所有服务列表."""
         return self._services.copy()
 
     @property
-    def services(self) -> dict[str, BaseService]:
+async def services(self) -> dict[str, BaseService]:
         """服务字典属性 - 兼容测试代码."""
         return self._services
 
@@ -106,7 +106,7 @@ }
 
 
-def _ensure_default_services() -> None:
+async def _ensure_default_services() -> None:
     settings = get_settings()
     enabled_services_str = getattr(settings, "enabled_services", "")
 
@@ -119,7 +119,7 @@         enabled_services = []
 
     for service_name in enabled_services:
-        factory = _SERVICE_FACTORIES.get(service_name)
+        factory = _SERVICE_FACTORIES.await get(service_name)
         if not factory:
             service_manager.logger.warning(
                 "未识别的服务名称,跳过注册: %s", service_name
