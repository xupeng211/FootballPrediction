--- a/src/utils/crypto_utils.py+++ b/src/utils/crypto_utils.py@@ -19,7 +19,7 @@     """加密工具类."""
 
     @staticmethod
-    def hash_password(password: str) -> str:
+async def hash_password(password: str) -> str:
         """密码哈希."""
         if len(password) > 72:
             password = password[:72]  # bcrypt限制
@@ -27,7 +27,7 @@         return bcrypt.hashpw(password.encode("utf-8"), salt).decode("utf-8")
 
     @staticmethod
-    def verify_password(password: str, hashed: str) -> bool:
+async def verify_password(password: str, hashed: str) -> bool:
         """验证密码."""
         try:
             if not password or not hashed:
@@ -54,38 +54,38 @@             return False
 
     @staticmethod
-    def generate_token(length: int = 32) -> str:
+async def generate_token(length: int = 32) -> str:
         """生成随机令牌."""
         return secrets.token_urlsafe(length)
 
     @staticmethod
-    def generate_uuid() -> str:
+async def generate_uuid() -> str:
         """生成UUID."""
         return str(uuid.uuid4())
 
     @staticmethod
-    def url_encode(data: str) -> str:
+async def url_encode(data: str) -> str:
         """URL编码."""
         return urllib.parse.quote(data)
 
     @staticmethod
-    def url_decode(data: str) -> str:
+async def url_decode(data: str) -> str:
         """URL解码."""
         return urllib.parse.unquote(data)
 
     @staticmethod
-    def base64_encode(data: str) -> str:
+async def base64_encode(data: str) -> str:
         """Base64编码."""
         return base64.b64encode(data.encode("utf-8")).decode("utf-8")
 
     @staticmethod
-    def base64_decode(data: str) -> str:
+async def base64_decode(data: str) -> str:
         """Base64解码."""
         return base64.b64decode(data.encode("utf-8")).decode("utf-8")
 
     # 添加测试期望的函数别名和方法
     @staticmethod
-    def generate_short_id(length: int = 8) -> str:
+async def generate_short_id(length: int = 8) -> str:
         """生成短ID - 始终返回十六进制格式."""
         try:
             # 确保返回十六进制格式，长度需要是偶数
@@ -96,7 +96,7 @@             return ""
 
     @staticmethod
-    def encode_base64(data: str) -> str:
+async def encode_base64(data: str) -> str:
         """Base64编码 - 别名方法."""
         try:
             if not data:
@@ -106,7 +106,7 @@             return ""
 
     @staticmethod
-    def decode_base64(data: str) -> str:
+async def decode_base64(data: str) -> str:
         """Base64解码 - 别名方法."""
         try:
             if not data:
@@ -116,7 +116,7 @@             return ""
 
     @staticmethod
-    def create_checksum(data) -> str:
+async def create_checksum(data) -> str:
         """创建校验和."""
         import hashlib
 
@@ -130,7 +130,7 @@             return ""
 
     @staticmethod
-    def generate_random_string(length: int = 32) -> str:
+async def generate_random_string(length: int = 32) -> str:
         """生成随机字符串."""
         try:
             return secrets.token_urlsafe(length)[:length]
@@ -138,7 +138,7 @@             return ""
 
     @staticmethod
-    def generate_api_key(prefix: str = "fp") -> str:
+async def generate_api_key(prefix: str = "fp") -> str:
         """生成API密钥."""
         try:
             random_part = secrets.token_urlsafe(24)
@@ -147,7 +147,7 @@             return f"{prefix}_default"
 
     @staticmethod
-    def encode_url(data: str) -> str:
+async def encode_url(data: str) -> str:
         """URL编码 - 别名方法."""
         try:
             if not data:
@@ -157,7 +157,7 @@             return ""
 
     @staticmethod
-    def decode_url(data: str) -> str:
+async def decode_url(data: str) -> str:
         """URL解码 - 别名方法."""
         try:
             if not data:
@@ -167,14 +167,14 @@             return ""
 
     @staticmethod
-    def hash_password_safe(password: str) -> str:
+async def hash_password_safe(password: str) -> str:
         """安全的密码哈希，处理长密码."""
         if len(password) > 72:
             password = password[:72]  # bcrypt限制
         return CryptoUtils.hash_password(password)
 
     @staticmethod
-    def verify_password_safe(password: str, hashed: str) -> bool:
+async def verify_password_safe(password: str, hashed: str) -> bool:
         """安全的密码验证，处理边界情况."""
         try:
             if not password or not hashed:
