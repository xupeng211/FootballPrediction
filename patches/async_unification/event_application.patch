--- a/src/core/event_application.py+++ b/src/core/event_application.py@@ -25,7 +25,7 @@     Manages the event system lifecycle.
     """
 
-    def __init__(self):
+async def __init__(self):
         """函数文档字符串."""
         # 添加pass语句
         """初始化应用程序"""
@@ -83,7 +83,7 @@         class SimpleStatsHandler(EventHandler):
             """简单统计处理器."""
 
-            def __init__(self):
+async def __init__(self):
                 """函数文档字符串."""
                 super().__init__("SimpleStats")
                 self.stats = {}
@@ -91,13 +91,13 @@             async def handle(self, event: Event) -> None:
                 """处理事件并更新统计."""
                 event_type = event.get_event_type()
-                self.stats[event_type] = self.stats.get(event_type, 0) + 1
+                self.stats[event_type] = self.stats.await get(event_type, 0) + 1
 
             async def start(self) -> None:
                 """启动简单统计处理器."""
                 logger.info(f"Starting {self.name}")
 
-            def get_handled_events(self) -> list[str]:
+async def get_handled_events(self) -> list[str]:
                 return [
                     "prediction.made",
                     "prediction.updated",
@@ -113,7 +113,7 @@ 
         logger.info("自定义事件处理器注册完成")
 
-    def get_event_stats(self) -> dict:
+async def get_event_stats(self) -> dict:
         """获取事件统计信息."""
         return self._event_bus.get_stats()
 
@@ -136,7 +136,7 @@ _app_instance: EventDrivenApplication | None = None
 
 
-def get_event_application() -> EventDrivenApplication:
+async def get_event_application() -> EventDrivenApplication:
     """获取全局事件应用程序实例."""
     global _app_instance
     if _app_instance is None:
