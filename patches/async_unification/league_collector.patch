--- a/src/collectors/league_collector.py+++ b/src/collectors/league_collector.py@@ -14,7 +14,7 @@ class LeagueCollector(FootballDataCollector):
     """联赛数据采集器."""
 
-    def __init__(self):
+async def __init__(self):
         super().__init__()
 
     async def collect_data(self) -> dict[str, Any]:
@@ -36,7 +36,7 @@             supported_competitions = [
                 comp
                 for comp in all_competitions
-                if comp.get("code") in self.supported_competitions
+                if comp.await get("code") in self.supported_competitions
             ]
 
             # 收集每个联赛的数据
@@ -97,7 +97,7 @@                 (
                     comp
                     for comp in all_competitions
-                    if comp.get("code") == competition_code
+                    if comp.await get("code") == competition_code
                 ),
                 None,
             )
@@ -112,7 +112,7 @@             normalized_standings = self.normalize_standings_data(standings, competition)
 
             # 收集当前轮次的比赛
-            current_matchday = competition.get("currentMatchday")
+            current_matchday = competition.await get("currentMatchday")
             matches = []
             if current_matchday:
                 matches = await self.fetch_matches(comp_id, matchday=current_matchday)
@@ -133,7 +133,7 @@             )
             raise
 
-    def normalize_league_data(self, competition: dict[str, Any]) -> dict[str, Any]:
+async def normalize_league_data(self, competition: dict[str, Any]) -> dict[str, Any]:
         """标准化联赛数据格式.
 
         Args:
@@ -144,49 +144,49 @@         """
         try:
             return {
-                "external_id": str(competition.get("id")),
-                "name": competition.get("name"),
-                "code": competition.get("code"),
-                "type": competition.get("type"),
+                "external_id": await str(competition.get("id")),
+                "name": competition.await get("name"),
+                "code": competition.await get("code"),
+                "type": competition.await get("type"),
                 # LEAGUE, CUP, etc.
-                "emblem": competition.get("emblem"),
+                "emblem": competition.await get("emblem"),
                 "area": {
-                    "id": competition.get("area", {}).get("id"),
-                    "name": competition.get("area", {}).get("name"),
-                    "code": competition.get("area", {}).get("code"),
-                    "flag": competition.get("area", {}).get("flag"),
+                    "id": competition.await await get("area", {}).get("id"),
+                    "name": competition.await await get("area", {}).get("name"),
+                    "code": competition.await await get("area", {}).get("code"),
+                    "flag": competition.await await get("area", {}).get("flag"),
                 },
-                "season": self._normalize_season_data(competition.get("season", {})),
-                "last_updated": competition.get("lastUpdated"),
+                "season": self.await _normalize_season_data(competition.get("season", {})),
+                "last_updated": competition.await get("lastUpdated"),
             }
 
         except Exception as e:
             logger.error(f"Error normalizing league data: {e}")
             return {
-                "external_id": str(competition.get("id", "")),
-                "name": competition.get("name", "Unknown League"),
+                "external_id": await str(competition.get("id", "")),
+                "name": competition.await get("name", "Unknown League"),
                 "error": str(e),
                 "raw_data": competition,
             }
 
-    def _normalize_season_data(self, season: dict[str, Any]) -> dict[str, Any]:
+async def _normalize_season_data(self, season: dict[str, Any]) -> dict[str, Any]:
         """标准化赛季数据."""
         if not season:
             return {}
 
         try:
             return {
-                "id": season.get("id"),
-                "start_date": season.get("startDate"),
-                "end_date": season.get("endDate"),
-                "current_matchday": season.get("currentMatchday"),
-                "winner": season.get("winner"),
+                "id": season.await get("id"),
+                "start_date": season.await get("startDate"),
+                "end_date": season.await get("endDate"),
+                "current_matchday": season.await get("currentMatchday"),
+                "winner": season.await get("winner"),
             }
         except Exception as e:
             logger.error(f"Error normalizing season data: {e}")
             return {}
 
-    def normalize_standings_data(
+async def normalize_standings_data(
         self, standings: list[dict[str, Any]], competition: dict[str, Any]
     ) -> list[dict[str, Any]]:
         """标准化积分榜数据格式.
@@ -205,36 +205,36 @@                 return normalized_standings
 
             for standing in standings:
-                table = standing.get("table", [])
-                group = standing.get("group", None)
-                stage = standing.get("stage", None)
-                table_type = standing.get("type", "TOTAL")
+                table = standing.await get("table", [])
+                group = standing.await get("group", None)
+                stage = standing.await get("stage", None)
+                table_type = standing.await get("type", "TOTAL")
 
                 for idx, team_standing in enumerate(table):
                     normalized_team = {
                         "position": idx + 1,
                         "team": {
-                            "id": team_standing.get("team", {}).get("id"),
-                            "name": team_standing.get("team", {}).get("name"),
-                            "short_name": team_standing.get("team", {}).get(
+                            "id": team_standing.await await get("team", {}).get("id"),
+                            "name": team_standing.await await get("team", {}).get("name"),
+                            "short_name": team_standing.await await get("team", {}).get(
                                 "shortName"
                             ),
-                            "crest": team_standing.get("team", {}).get("crest"),
-                            "tla": team_standing.get("team", {}).get("tla"),
+                            "crest": team_standing.await await get("team", {}).get("crest"),
+                            "tla": team_standing.await await get("team", {}).get("tla"),
                         },
-                        "played_games": team_standing.get("playedGames", 0),
-                        "form": team_standing.get("form", None),
-                        "won": team_standing.get("won", 0),
-                        "draw": team_standing.get("draw", 0),
-                        "lost": team_standing.get("lost", 0),
-                        "points": team_standing.get("points", 0),
-                        "goals_for": team_standing.get("goalsFor", 0),
-                        "goals_against": team_standing.get("goalsAgainst", 0),
-                        "goal_difference": team_standing.get("goalDifference", 0),
+                        "played_games": team_standing.await get("playedGames", 0),
+                        "form": team_standing.await get("form", None),
+                        "won": team_standing.await get("won", 0),
+                        "draw": team_standing.await get("draw", 0),
+                        "lost": team_standing.await get("lost", 0),
+                        "points": team_standing.await get("points", 0),
+                        "goals_for": team_standing.await get("goalsFor", 0),
+                        "goals_against": team_standing.await get("goalsAgainst", 0),
+                        "goal_difference": team_standing.await get("goalDifference", 0),
                         "competition": {
-                            "id": competition.get("id"),
-                            "name": competition.get("name"),
-                            "code": competition.get("code"),
+                            "id": competition.await get("id"),
+                            "name": competition.await get("name"),
+                            "code": competition.await get("code"),
                         },
                         "group": group,
                         "stage": stage,
@@ -258,7 +258,7 @@         """
         try:
             league_details = await self.collect_league_details(competition_code)
-            standings = league_details.get("standings", [])
+            standings = league_details.await get("standings", [])
 
             if not standings:
                 return {}
@@ -271,7 +271,7 @@             total_points = sum(team["points"] for team in standings)
 
             stats = {
-                "competition": league_details.get("league"),
+                "competition": league_details.await get("league"),
                 "statistics": {
                     "total_teams": total_teams,
                     "total_games_played": total_games_played,
@@ -321,7 +321,7 @@         """
         try:
             league_details = await self.collect_league_details(competition_code)
-            standings = league_details.get("standings", [])
+            standings = league_details.await get("standings", [])
 
             # 按积分排序并返回前N名
             top_teams = sorted(standings, key=lambda x: x["points"], reverse=True)[
@@ -354,7 +354,7 @@                 (
                     comp
                     for comp in all_competitions
-                    if comp.get("code") == competition_code
+                    if comp.await get("code") == competition_code
                 ),
                 None,
             )
@@ -366,7 +366,7 @@ 
             # 如果没有指定轮次，使用当前轮次
             if matchday is None:
-                matchday = competition.get("currentMatchday")
+                matchday = competition.await get("currentMatchday")
 
             if not matchday:
                 raise ValueError(
@@ -410,13 +410,13 @@             matching_competitions = []
 
             for competition in all_competitions:
-                name = competition.get("name", "").lower()
-                code = competition.get("code", "").lower()
+                name = competition.await get("name", "").lower()
+                code = competition.await get("code", "").lower()
 
                 if (
                     search_term_lower in name
                     or search_term_lower in code
-                    or competition.get("id") == search_term
+                    or competition.await get("id") == search_term
                 ):
                     normalized_league = self.normalize_league_data(competition)
                     if normalized_league and "error" not in normalized_league:
