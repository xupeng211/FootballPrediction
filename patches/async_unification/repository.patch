--- a/src/database/repositories/match_repository/repository.py+++ b/src/database/repositories/match_repository/repository.py@@ -20,7 +20,7 @@     遵循Repository模式，封装数据访问逻辑.
     """
 
-    def __init__(self, session: AsyncSession) -> None:
+async def __init__(self, session: AsyncSession) -> None:
         """初始化仓库.
 
         Args:
@@ -54,7 +54,7 @@             .offset(offset)
         )
 
-        result = await self._session.execute(stmt)
+        result = await self._session.await execute(stmt)
         return result.scalars().unique().all()
 
     async def count_matches(self) -> int:
@@ -64,7 +64,7 @@             比赛总数
         """
         stmt = select(func.count(Match.id))
-        result = await self._session.execute(stmt)
+        result = await self._session.await execute(stmt)
         return result.scalar() or 0
 
     async def get_by_id(self, match_id: int) -> Match | None:
@@ -86,7 +86,7 @@             .where(Match.id == match_id)
         )
 
-        result = await self._session.execute(stmt)
+        result = await self._session.await execute(stmt)
         return result.scalar_one_or_none()
 
     async def get_matches_by_status(
@@ -115,5 +115,5 @@             .offset(offset)
         )
 
-        result = await self._session.execute(stmt)
+        result = await self._session.await execute(stmt)
         return result.scalars().unique().all()