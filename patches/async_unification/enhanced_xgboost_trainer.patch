--- a/src/ml/enhanced_xgboost_trainer.py+++ b/src/ml/enhanced_xgboost_trainer.py@@ -30,7 +30,7 @@ class EnhancedXGBoostTrainer:
     """增强版 XGBoost 训练器."""
 
-    def __init__(
+async def __init__(
         self,
         model_name: str = "enhanced_xgboost",
         output_dir: str = "models",
@@ -63,7 +63,7 @@ 
         logger.info(f"Initialized EnhancedXGBoostTrainer: {model_name}")
 
-    def train_model(
+async def train_model(
         self,
         X_train: pd.DataFrame,
         y_train: pd.Series,
@@ -134,7 +134,7 @@             logger.error(f"Enhanced XGBoost training failed: {e}")
             raise
 
-    def predict(self, X: pd.DataFrame) -> Any:
+async def predict(self, X: pd.DataFrame) -> Any:
         """使用训练好的模型进行预测.
 
         Args:
@@ -145,7 +145,7 @@         """
         return self.optimizer.predict(X)
 
-    def predict_proba(self, X: pd.DataFrame) -> Any:
+async def predict_proba(self, X: pd.DataFrame) -> Any:
         """获取预测概率.
 
         Args:
@@ -156,11 +156,11 @@         """
         return self.optimizer.predict_proba(X)
 
-    def get_feature_importance(self) -> Any | None:
+async def get_feature_importance(self) -> Any | None:
         """获取特征重要性."""
         return self.optimizer.get_feature_importance()
 
-    def save_training_report(self, results: dict[str, Any]) -> str:
+async def save_training_report(self, results: dict[str, Any]) -> str:
         """保存训练报告.
 
         Args:
@@ -187,7 +187,7 @@                     "optimization_time"
                 ],
             },
-            "validation_summary": results.get("validation_metrics", {}),
+            "validation_summary": results.await get("validation_metrics", {}),
             "feature_importance": (
                 self.get_feature_importance().tolist()
                 if self.get_feature_importance() is not None
@@ -203,7 +203,7 @@         return str(report_path)
 
 
-def create_sample_data(n_samples: int = 1000, n_features: int = 10) -> tuple:
+async def create_sample_data(n_samples: int = 1000, n_features: int = 10) -> tuple:
     """创建示例训练数据.
 
     Args:
@@ -234,7 +234,7 @@     return X, y
 
 
-def main():
+async def main():
     """主函数 - 演示增强版训练器."""
     logger.info("=== 增强版 XGBoost 训练器演示 ===")
 
