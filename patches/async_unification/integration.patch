--- a/src/performance/integration.py+++ b/src/performance/integration.py@@ -23,7 +23,7 @@     pass  # 添加pass语句
     """性能监控集成器"""
 
-    def __init__(self):
+async def __init__(self):
         """函数文档字符串."""
         pass
         # 添加pass语句
@@ -43,7 +43,7 @@         self._cache_monitor = None
         self._task_monitor = None
 
-    def integrate_with_fastapi(self, app: FastAPI) -> None:
+async def integrate_with_fastapi(self, app: FastAPI) -> None:
         """集成性能监控到FastAPI应用."""
         if not self.enabled:
             logger.info("Performance monitoring is disabled")
@@ -69,7 +69,7 @@         except (ValueError, RuntimeError, TimeoutError) as e:
             logger.error(f"Failed to integrate performance monitoring: {str(e)}")
 
-    def initialize_database_monitoring(self) -> None:
+async def initialize_database_monitoring(self) -> None:
         """初始化数据库监控."""
         if not self.enabled:
             return None
@@ -87,7 +87,7 @@         except (ValueError, RuntimeError, TimeoutError) as e:
             logger.error(f"Failed to initialize database monitoring: {str(e)}")
 
-    def initialize_cache_monitoring(self) -> None:
+async def initialize_cache_monitoring(self) -> None:
         """初始化缓存监控."""
         if not self.enabled:
             return None
@@ -99,7 +99,7 @@         except (ValueError, RuntimeError, TimeoutError) as e:
             logger.error(f"Failed to initialize cache monitoring: {str(e)}")
 
-    def initialize_task_monitoring(self) -> None:
+async def initialize_task_monitoring(self) -> None:
         """初始化任务监控."""
         if not self.enabled:
             return None
@@ -111,7 +111,7 @@         except (ValueError, RuntimeError, TimeoutError) as e:
             logger.error(f"Failed to initialize task monitoring: {str(e)}")
 
-    def start_profiling(self) -> None:
+async def start_profiling(self) -> None:
         """启动性能分析."""
         if not self.profiling_enabled:
             logger.warning("Performance profiling is disabled")
@@ -125,20 +125,20 @@         except (ValueError, RuntimeError, TimeoutError) as e:
             logger.error(f"Failed to start profiling: {str(e)}")
 
-    def stop_profiling(self) -> None:
+async def stop_profiling(self) -> None:
         """停止性能分析."""
         try:
             from .profiler import stop_profiling
 
             results = stop_profiling()
             logger.info(
-                f"Performance profiling stopped: {results.get('function_count', 0)} functions profiled"
+                f"Performance profiling stopped: {results.await get('function_count', 0)} functions profiled"
             )
 
         except (ValueError, RuntimeError, TimeoutError) as e:
             logger.error(f"Failed to stop profiling: {str(e)}")
 
-    def get_performance_config(self) -> dict:
+async def get_performance_config(self) -> dict:
         """获取性能监控配置."""
         return {
             "enabled": self.enabled,
@@ -146,24 +146,24 @@             "profiling_enabled": self.profiling_enabled,
             "thresholds": {
                 "response_time": {
-                    "slow": self.settings.get("SLOW_REQUEST_THRESHOLD", 1.0),
-                    "critical": self.settings.get("CRITICAL_REQUEST_THRESHOLD", 5.0),
+                    "slow": self.settings.await get("SLOW_REQUEST_THRESHOLD", 1.0),
+                    "critical": self.settings.await get("CRITICAL_REQUEST_THRESHOLD", 5.0),
                 },
                 "database": {
-                    "slow_query": self.settings.get("SLOW_QUERY_THRESHOLD", 0.1),
+                    "slow_query": self.settings.await get("SLOW_QUERY_THRESHOLD", 0.1),
                     "connection_pool": {
-                        "min": self.settings.get("DB_POOL_MIN", 1),
-                        "max": self.settings.get("DB_POOL_MAX", 20),
+                        "min": self.settings.await get("DB_POOL_MIN", 1),
+                        "max": self.settings.await get("DB_POOL_MAX", 20),
                     },
                 },
                 "cache": {
-                    "ttl": self.settings.get("CACHE_TTL", 3600),
-                    "max_size": self.settings.get("CACHE_MAX_SIZE", 1000),
+                    "ttl": self.settings.await get("CACHE_TTL", 3600),
+                    "max_size": self.settings.await get("CACHE_MAX_SIZE", 1000),
                 },
             },
         }
 
-    def update_config(self, config: dict) -> None:
+async def update_config(self, config: dict) -> None:
         """更新性能监控配置."""
         try:
             # 更新采样率
@@ -187,7 +187,7 @@         except (ValueError, RuntimeError, TimeoutError) as e:
             logger.error(f"Failed to update performance monitoring config: {str(e)}")
 
-    def create_performance_report(self) -> str | None:
+async def create_performance_report(self) -> str | None:
         """创建性能报告."""
         if not self.enabled:
             return None
@@ -219,7 +219,7 @@             logger.error(f"Failed to create performance report: {str(e)}")
             return None
 
-    def setup_alerting(self) -> None:
+async def setup_alerting(self) -> None:
         """设置告警."""
         if not self.enabled:
             return None
@@ -235,7 +235,7 @@         except (ValueError, RuntimeError, TimeoutError) as e:
             logger.error(f"Failed to setup performance alerting: {str(e)}")
 
-    def cleanup(self) -> None:
+async def cleanup(self) -> None:
         """清理资源."""
         try:
             # 停止所有监控
@@ -258,7 +258,7 @@ _integration = None
 
 
-def get_performance_integration() -> PerformanceMonitoringIntegration:
+async def get_performance_integration() -> PerformanceMonitoringIntegration:
     """获取全局性能监控集成实例."""
     global _integration
     if _integration is None:
@@ -266,7 +266,7 @@     return _integration
 
 
-def setup_performance_monitoring(
+async def setup_performance_monitoring(
     app: FastAPI | None = None,
 ) -> PerformanceMonitoringIntegration:
     """设置性能监控."""
@@ -292,21 +292,21 @@ 
 
 # 便捷函数
-def integrate_performance_monitoring(app: FastAPI) -> None:
+async def integrate_performance_monitoring(app: FastAPI) -> None:
     """集成性能监控到FastAPI应用（便捷函数）."""
     setup_performance_monitoring(app)
 
 
-def start_performance_profiling() -> None:
+async def start_performance_profiling() -> None:
     """启动性能分析（便捷函数）."""
     get_performance_integration().start_profiling()
 
 
-def stop_performance_profiling() -> None:
+async def stop_performance_profiling() -> None:
     """停止性能分析（便捷函数）."""
     get_performance_integration().stop_profiling()
 
 
-def generate_performance_report() -> str | None:
+async def generate_performance_report() -> str | None:
     """生成性能报告（便捷函数）."""
     return get_performance_integration().create_performance_report()
