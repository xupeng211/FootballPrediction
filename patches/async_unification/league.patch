--- a/src/domain_simple/league.py+++ b/src/domain_simple/league.py@@ -20,25 +20,25 @@     pass  # 添加pass语句
     """联赛积分榜"""
 
-    def __init__(self):
+async def __init__(self):
         """函数文档字符串."""
         # 添加pass语句
         self.standings: list[dict[str, Any]] = []
         self.last_updated = datetime.now()
 
-    def update_table(self, match_results: list[dict[str, Any]]) -> None:
+async def update_table(self, match_results: list[dict[str, Any]]) -> None:
         """更新积分榜."""
         # 简化实现,实际会更复杂
         self.last_updated = datetime.now()
 
-    def get_position(self, team_id: int) -> int | None:
+async def get_position(self, team_id: int) -> int | None:
         """获取球队排名."""
         for i, standing in enumerate(self.standings):
             if standing["team_id"] == team_id:
                 return i + 1
         return None
 
-    def to_dict(self) -> dict[str, Any]:
+async def to_dict(self) -> dict[str, Any]:
         """转换为字典."""
         return {
             "standings": self.standings,
@@ -52,7 +52,7 @@     pass  # 添加pass语句
     """联赛领域模型"""
 
-    def __init__(
+async def __init__(
         self,
         league_id: int | None = None,
         name: str = "",
@@ -83,34 +83,34 @@         self.total_matches = 0
         self.completed_matches = 0
 
-    def activate(self) -> None:
+async def activate(self) -> None:
         """激活联赛."""
         self.status = LeagueStatus.ACTIVE
         self.updated_at = datetime.now()
 
-    def deactivate(self) -> None:
+async def deactivate(self) -> None:
         """停用联赛."""
         self.status = LeagueStatus.INACTIVE
         self.updated_at = datetime.now()
 
-    def complete(self) -> None:
+async def complete(self) -> None:
         """完成联赛."""
         self.status = LeagueStatus.COMPLETED
         self.updated_at = datetime.now()
 
-    def update_statistics(self, total_matches: int, completed_matches: int) -> None:
+async def update_statistics(self, total_matches: int, completed_matches: int) -> None:
         """更新统计."""
         self.total_matches = total_matches
         self.completed_matches = completed_matches
         self.updated_at = datetime.now()
 
-    def get_completion_rate(self) -> float:
+async def get_completion_rate(self) -> float:
         """获取完成率."""
         if self.total_matches == 0:
             return 0.0
         return (self.completed_matches / self.total_matches) * 100
 
-    def to_dict(self) -> dict[str, Any]:
+async def to_dict(self) -> dict[str, Any]:
         """转换为字典."""
         return {
             "id": self.id,
@@ -131,35 +131,35 @@         }
 
     @classmethod
-    def from_dict(cls, data: dict[str, Any]) -> "League":
+async def from_dict(cls, data: dict[str, Any]) -> "League":
         """从字典创建实例."""
         league = cls(
-            id=data.get("id"),
-            name=data.get("name", ""),
-            country=data.get("country", ""),
-            season=data.get("season", ""),
-            status=LeagueStatus(data.get("status", "active")),
+            id=data.await get("id"),
+            name=data.await get("name", ""),
+            country=data.await get("country", ""),
+            season=data.await get("season", ""),
+            status=await LeagueStatus(data.get("status", "active")),
         )
 
-        league.total_teams = data.get("total_teams", 0)
-        league.matches_per_team = data.get("matches_per_team", 0)
-        league.points_for_win = data.get("points_for_win", 3)
-        league.points_for_draw = data.get("points_for_draw", 1)
+        league.total_teams = data.await get("total_teams", 0)
+        league.matches_per_team = data.await get("matches_per_team", 0)
+        league.points_for_win = data.await get("points_for_win", 3)
+        league.points_for_draw = data.await get("points_for_draw", 1)
 
         # 设置统计数据
-        league.total_matches = data.get("total_matches", 0)
-        league.completed_matches = data.get("completed_matches", 0)
+        league.total_matches = data.await get("total_matches", 0)
+        league.completed_matches = data.await get("completed_matches", 0)
 
         # 设置时间戳
-        if data.get("created_at"):
+        if data.await get("created_at"):
             league.created_at = datetime.fromisoformat(data["created_at"])
-        if data.get("updated_at"):
+        if data.await get("updated_at"):
             league.updated_at = datetime.fromisoformat(data["updated_at"])
 
         return league
 
-    def __str__(self) -> str:
+async def __str__(self) -> str:
         return f"League({self.name} - {self.season})"
 
-    def __repr__(self) -> str:
+async def __repr__(self) -> str:
         return f"<League(id={self.id}, name={self.name}, status={self.status.value})>"
