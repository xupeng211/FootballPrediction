--- a/src/cache/ttl_cache_enhanced/cache_factory.py+++ b/src/cache/ttl_cache_enhanced/cache_factory.py@@ -20,7 +20,7 @@     """
 
     @staticmethod
-    def create_cache(
+async def create_cache(
         cache_type: str = "sync",
         max_size: int = 1000,
         default_ttl: float | None = None,
@@ -48,7 +48,7 @@             raise ValueError(f"不支持的缓存类型: {cache_type}")
 
     @staticmethod
-    def create_lru_cache(max_size: int = 128) -> TTLCache:
+async def create_lru_cache(max_size: int = 128) -> TTLCache:
         """创建LRU缓存.
 
         Args:
@@ -60,7 +60,7 @@         return TTLCache(max_size=max_size)
 
     @staticmethod
-    def create_ttl_cache(
+async def create_ttl_cache(
         max_size: int = 1000,
         default_ttl: float = 3600,
     ) -> TTLCache:
@@ -76,7 +76,7 @@         return TTLCache(max_size=max_size, default_ttl=default_ttl)
 
     @staticmethod
-    def create_async_lru_cache(max_size: int = 128) -> AsyncTTLCache:
+async def create_async_lru_cache(max_size: int = 128) -> AsyncTTLCache:
         """创建异步LRU缓存.
 
         Args:
@@ -88,7 +88,7 @@         return AsyncTTLCache(max_size=max_size)
 
     @staticmethod
-    def create_async_ttl_cache(
+async def create_async_ttl_cache(
         max_size: int = 1000,
         default_ttl: float = 3600,
     ) -> AsyncTTLCache:
@@ -104,7 +104,7 @@         return AsyncTTLCache(max_size=max_size, default_ttl=default_ttl)
 
     @staticmethod
-    def create_prediction_cache() -> TTLCache:
+async def create_prediction_cache() -> TTLCache:
         """创建预测缓存（预设配置）.
 
         Returns:
@@ -113,7 +113,7 @@         return TTLCache(max_size=10000, default_ttl=1800)  # 30分钟
 
     @staticmethod
-    def create_feature_cache() -> TTLCache:
+async def create_feature_cache() -> TTLCache:
         """创建特征缓存（预设配置）.
 
         Returns:
@@ -122,7 +122,7 @@         return TTLCache(max_size=5000, default_ttl=3600)  # 1小时
 
     @staticmethod
-    def create_odds_cache() -> TTLCache:
+async def create_odds_cache() -> TTLCache:
         """创建赔率缓存（预设配置）.
 
         Returns:
@@ -131,7 +131,7 @@         return TTLCache(max_size=20000, default_ttl=300)  # 5分钟
 
     @staticmethod
-    def create_session_cache() -> TTLCache:
+async def create_session_cache() -> TTLCache:
         """创建会话缓存（预设配置）.
 
         Returns:
@@ -140,7 +140,7 @@         return TTLCache(max_size=1000, default_ttl=7200)  # 2小时
 
     @staticmethod
-    def create_config_cache() -> TTLCache:
+async def create_config_cache() -> TTLCache:
         """创建配置缓存（预设配置）.
 
         Returns:
@@ -149,7 +149,7 @@         return TTLCache(max_size=500, default_ttl=86400)  # 24小时
 
     @staticmethod
-    def get_available_types() -> list:
+async def get_available_types() -> list:
         """获取可用的缓存类型.
 
         Returns:
@@ -158,7 +158,7 @@         return ["sync", "async"]
 
     @staticmethod
-    def get_preset_configs() -> dict:
+async def get_preset_configs() -> dict:
         """获取预设配置.
 
         Returns:
