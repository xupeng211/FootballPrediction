--- a/src/services/content_analysis_service.py+++ b/src/services/content_analysis_service.py@@ -20,12 +20,12 @@ class ContentAnalysisService:
     """内容分析服务."""
 
-    def __init__(self, session=None):
+async def __init__(self, session=None):
         """初始化内容分析服务."""
         self.session = session
         self.analyzers = {}
 
-    def analyze_text(
+async def analyze_text(
         self, text: str, analysis_type: str = "sentiment"
     ) -> ContentAnalysisResult:
         """分析文本内容.
@@ -50,7 +50,7 @@ 
         return result
 
-    def analyze_content(
+async def analyze_content(
         self, content: Any, content_type: str = "text"
     ) -> ContentAnalysisResult:
         """分析内容.
