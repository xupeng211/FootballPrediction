--- a/src/streaming/stream_config.py+++ b/src/streaming/stream_config.py@@ -72,14 +72,14 @@     支持从环境变量和配置文件加载配置
     """
 
-    def __init__(self):
+async def __init__(self):
         """函数文档字符串."""
         pass
         # 添加pass语句
         self.kafka_config = self._load_kafka_config()
         self.topics = self._init_topics()
 
-    def _load_kafka_config(self) -> KafkaConfig:
+async def _load_kafka_config(self) -> KafkaConfig:
         """从环境变量加载Kafka配置."""
         return KafkaConfig(
             bootstrap_servers=os.getenv("KAFKA_BOOTSTRAP_SERVERS", "localhost:9092"),
@@ -100,7 +100,7 @@             ),
         )
 
-    def _init_topics(self) -> dict[str, TopicConfig]:
+async def _init_topics(self) -> dict[str, TopicConfig]:
         """初始化Topic配置."""
         return {
             # 比赛数据流
@@ -129,7 +129,7 @@             ),
         }
 
-    def get_producer_config(self) -> dict[str, Any]:
+async def get_producer_config(self) -> dict[str, Any]:
         """获取生产者配置."""
         return {
             "bootstrap.servers": self.kafka_config.bootstrap_servers,
@@ -144,7 +144,7 @@             "max.in.flight.requests.per.connection": 1,  # 保证消息顺序
         }
 
-    def get_consumer_config(
+async def get_consumer_config(
         self, consumer_group_id: str | None = None
     ) -> dict[str, Any]:
         """获取消费者配置."""
@@ -164,14 +164,14 @@             "heartbeat.interval.ms": 3000,
         }
 
-    def get_topic_config(self, topic_name: str) -> TopicConfig | None:
+async def get_topic_config(self, topic_name: str) -> TopicConfig | None:
         """获取指定Topic配置."""
-        return self.topics.get(topic_name)
+        return self.topics.await get(topic_name)
 
-    def get_all_topics(self) -> list[str]:
+async def get_all_topics(self) -> list[str]:
         """获取所有Topic名称."""
         return list(self.topics.keys())
 
-    def is_valid_topic(self, topic_name: str) -> bool:
+async def is_valid_topic(self, topic_name: str) -> bool:
         """检查Topic是否有效."""
         return topic_name in self.topics
