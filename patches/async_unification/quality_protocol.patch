--- a/src/quality/quality_protocol.py+++ b/src/quality/quality_protocol.py@@ -78,7 +78,7 @@     所有结果都应该是 JSON-safe 的，便于序列化和存储。
     """
 
-    def __init__(
+async def __init__(
         self,
         rule_name: str,
         passed: bool,
@@ -102,7 +102,7 @@         self.severity = severity
         self.metadata = metadata or {}
 
-    def to_dict(self) -> dict[str, Any]:
+async def to_dict(self) -> dict[str, Any]:
         """转换为字典格式，确保 JSON-safe。"""
         return {
             "rule_name": self.rule_name,
@@ -198,7 +198,7 @@ class RuleFactory(Protocol):
     """规则工厂协议，用于创建和配置数据质量规则实例。"""
 
-    def create_rule(self, rule_type: str, config: dict[str, Any]) -> DataQualityRule:
+async def create_rule(self, rule_type: str, config: dict[str, Any]) -> DataQualityRule:
         """
         根据规则类型和配置创建规则实例。
 
@@ -225,7 +225,7 @@ }
 
 
-def get_rule_class(rule_type: str) -> type[DataQualityRule]:
+async def get_rule_class(rule_type: str) -> type[DataQualityRule]:
     """
     根据规则类型获取对应的规则类。
 
