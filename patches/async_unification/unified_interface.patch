--- a/src/cache/unified_interface.py+++ b/src/cache/unified_interface.py@@ -43,34 +43,34 @@     """缓存接口抽象基类."""
 
     @abstractmethod
-    def get(self, key: str, default: Any = None) -> Any:
+async def get(self, key: str, default: Any = None) -> Any:
         """获取缓存值."""
 
     @abstractmethod
-    def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
+async def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
         """设置缓存值."""
 
     @abstractmethod
-    def delete(self, key: str) -> bool:
+async def delete(self, key: str) -> bool:
         """删除缓存项."""
 
     @abstractmethod
-    def exists(self, key: str) -> bool:
+async def exists(self, key: str) -> bool:
         """检查键是否存在."""
 
     @abstractmethod
-    def clear(self) -> None:
+async def clear(self) -> None:
         """清空缓存."""
 
     @abstractmethod
-    def size(self) -> int:
+async def size(self) -> int:
         """获取缓存大小."""
 
 
 class MemoryCacheAdapter(CacheInterface):
     """内存缓存适配器."""
 
-    def __init__(self, config: dict[str, Any] = None):
+async def __init__(self, config: dict[str, Any] = None):
         default_config = {
             "max_size": 1000,
             "default_ttl": 3600,
@@ -79,39 +79,39 @@         final_config = {**default_config, **(config or {})}
         self._cache = TTLCache(**final_config)
 
-    def get(self, key: str, default: Any = None) -> Any:
-        return self._cache.get(key, default)
-
-    def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
+async def get(self, key: str, default: Any = None) -> Any:
+        return self._cache.await get(key, default)
+
+async def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
         try:
             self._cache.set(key, value, ttl)
             return True
         except Exception:
             return False
 
-    def delete(self, key: str) -> bool:
+async def delete(self, key: str) -> bool:
         return self._cache.delete(key)
 
-    def exists(self, key: str) -> bool:
-        return self._cache.get(key) is not None
-
-    def clear(self) -> None:
+async def exists(self, key: str) -> bool:
+        return self._cache.await get(key) is not None
+
+async def clear(self) -> None:
         self._cache.clear()
 
-    def size(self) -> int:
+async def size(self) -> int:
         return self._cache.size()
 
-    def get_stats(self) -> dict[str, Any]:
+async def get_stats(self) -> dict[str, Any]:
         return self._cache.get_stats()
 
 
 class RedisCacheAdapter(CacheInterface):
     """Redis缓存适配器."""
 
-    def __init__(self, config: RedisConfig = None, use_mock: bool = None):
+async def __init__(self, config: RedisConfig = None, use_mock: bool = None):
         self._manager = EnhancedRedisManager(config, use_mock)
 
-    def _serialize_value(self, value: Any) -> str:
+async def _serialize_value(self, value: Any) -> str:
         """序列化值."""
         if isinstance(value, str):
             return value
@@ -127,7 +127,7 @@ 
             return base64.b64encode(pickle.dumps(value)).decode()
 
-    def _deserialize_value(self, value: str) -> Any:
+async def _deserialize_value(self, value: str) -> Any:
         """反序列化值."""
         # 尝试解析为数字
         try:
@@ -157,9 +157,9 @@         # 返回原始字符串
         return value
 
-    def get(self, key: str, default: Any = None) -> Any:
+async def get(self, key: str, default: Any = None) -> Any:
         try:
-            value = self._manager.get(key)
+            value = self._manager.await get(key)
             if value is None:
                 return default
             # 反序列化失败时应该返回原始字符串，而不是抛出异常
@@ -172,7 +172,7 @@             logger.error(f"Redis获取失败: {e}")
             return default
 
-    def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
+async def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
         try:
             serialized = self._serialize_value(value)
             if ttl:
@@ -183,39 +183,39 @@             logger.error(f"Redis设置失败: {e}")
             return False
 
-    def delete(self, key: str) -> bool:
+async def delete(self, key: str) -> bool:
         return bool(self._manager.delete(key))
 
-    def exists(self, key: str) -> bool:
+async def exists(self, key: str) -> bool:
         return self._manager.exists(key)
 
-    def clear(self) -> None:
+async def clear(self) -> None:
         self._manager.flushdb()
 
-    def size(self) -> int:
+async def size(self) -> int:
         return len(self._manager.keys())
 
-    def get_stats(self) -> dict[str, Any]:
+async def get_stats(self) -> dict[str, Any]:
         return {"redis_info": self._manager.info(), "ping": self._manager.ping()}
 
 
 class MultiLevelCacheAdapter(CacheInterface):
     """多级缓存适配器."""
 
-    def __init__(
+async def __init__(
         self, memory_config: dict[str, Any] = None, redis_config: RedisConfig = None
     ):
         self._l1_cache = MemoryCacheAdapter(memory_config)
         self._l2_cache = RedisCacheAdapter(redis_config, use_mock=True)
 
-    def get(self, key: str, default: Any = None) -> Any:
+async def get(self, key: str, default: Any = None) -> Any:
         # 先查L1缓存
-        value = self._l1_cache.get(key)
+        value = self._l1_cache.await get(key)
         if value is not None:
             return value
 
         # 查L2缓存
-        value = self._l2_cache.get(key)
+        value = self._l2_cache.await get(key)
         if value is not None:
             # 回填到L1缓存
             self._l1_cache.set(key, value)
@@ -223,28 +223,28 @@ 
         return default
 
-    def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
+async def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
         l1_success = self._l1_cache.set(key, value, ttl)
         l2_success = self._l2_cache.set(key, value, ttl)
         return l1_success and l2_success
 
-    def delete(self, key: str) -> bool:
+async def delete(self, key: str) -> bool:
         l1_success = self._l1_cache.delete(key)
         l2_success = self._l2_cache.delete(key)
         return l1_success or l2_success
 
-    def exists(self, key: str) -> bool:
+async def exists(self, key: str) -> bool:
         return self._l1_cache.exists(key) or self._l2_cache.exists(key)
 
-    def clear(self) -> None:
+async def clear(self) -> None:
         self._l1_cache.clear()
         self._l2_cache.clear()
 
-    def size(self) -> int:
+async def size(self) -> int:
         # 返回L1缓存大小（实际缓存的数据量）
         return self._l1_cache.size()
 
-    def get_stats(self) -> dict[str, Any]:
+async def get_stats(self) -> dict[str, Any]:
         return {
             "l1_stats": self._l1_cache.get_stats(),
             "l2_stats": self._l2_cache.get_stats(),
@@ -254,7 +254,7 @@ class UnifiedCacheManager:
     """统一缓存管理器."""
 
-    def __init__(self, config: UnifiedCacheConfig = None):
+async def __init__(self, config: UnifiedCacheConfig = None):
         self.config = config or UnifiedCacheConfig()
         self._adapter = self._create_adapter()
         self._consistency_manager = None
@@ -264,7 +264,7 @@ 
         logger.info(f"统一缓存管理器初始化完成，后端: {self.config.backend.value}")
 
-    def _create_adapter(self) -> CacheInterface:
+async def _create_adapter(self) -> CacheInterface:
         """创建缓存适配器."""
         if self.config.backend == CacheBackend.MEMORY:
             return MemoryCacheAdapter(self.config.memory_config)
@@ -278,11 +278,11 @@             raise ValueError(f"不支持的缓存后端: {self.config.backend}")
 
     # 基础操作
-    def get(self, key: str, default: Any = None) -> Any:
+async def get(self, key: str, default: Any = None) -> Any:
         """获取缓存值."""
-        return self._adapter.get(key, default)
-
-    def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
+        return self._adapter.await get(key, default)
+
+async def set(self, key: str, value: Any, ttl: int | None = None) -> bool:
         """设置缓存值."""
         success = self._adapter.set(key, value, ttl)
 
@@ -309,7 +309,7 @@ 
         return success
 
-    def delete(self, key: str) -> bool:
+async def delete(self, key: str) -> bool:
         """删除缓存项."""
         success = self._adapter.delete(key)
 
@@ -328,11 +328,11 @@ 
         return success
 
-    def exists(self, key: str) -> bool:
+async def exists(self, key: str) -> bool:
         """检查键是否存在."""
         return self._adapter.exists(key)
 
-    def clear(self) -> None:
+async def clear(self) -> None:
         """清空缓存."""
         self._adapter.clear()
 
@@ -362,21 +362,21 @@                     # 忽略一致性管理器的错误，不影响缓存清空
                     pass
 
-    def size(self) -> int:
+async def size(self) -> int:
         """获取缓存大小."""
         return self._adapter.size()
 
     # 批量操作
-    def get_many(self, keys: list[str]) -> dict[str, Any]:
+async def get_many(self, keys: list[str]) -> dict[str, Any]:
         """批量获取缓存值."""
         result = {}
         for key in keys:
-            value = self.get(key)
+            value = self.await get(key)
             if value is not None:
                 result[key] = value
         return result
 
-    def set_many(self, mapping: dict[str, Any], ttl: int | None = None) -> bool:
+async def set_many(self, mapping: dict[str, Any], ttl: int | None = None) -> bool:
         """批量设置缓存值."""
         success_count = 0
         for key, value in mapping.items():
@@ -384,7 +384,7 @@                 success_count += 1
         return success_count == len(mapping)
 
-    def delete_many(self, keys: list[str]) -> int:
+async def delete_many(self, keys: list[str]) -> int:
         """批量删除缓存项."""
         success_count = 0
         for key in keys:
@@ -393,20 +393,20 @@         return success_count
 
     # 高级操作
-    def incr(self, key: str, amount: int = 1) -> int:
+async def incr(self, key: str, amount: int = 1) -> int:
         """递增数值."""
-        current = self.get(key, 0)
+        current = self.await get(key, 0)
         if not isinstance(current, (int, float)):
             current = 0
         new_value = current + amount
         self.set(key, new_value)
         return new_value
 
-    def decr(self, key: str, amount: int = 1) -> int:
+async def decr(self, key: str, amount: int = 1) -> int:
         """递减数值."""
         return self.incr(key, -amount)
 
-    def ttl(self, key: str) -> int:
+async def ttl(self, key: str) -> int:
         """获取键的TTL."""
         if hasattr(self._adapter, "_cache") and hasattr(self._adapter._cache, "ttl"):
             return self._adapter._cache.ttl(key)
@@ -415,20 +415,20 @@         else:
             return -1
 
-    def expire(self, key: str, seconds: int) -> bool:
+async def expire(self, key: str, seconds: int) -> bool:
         """设置键的过期时间."""
-        value = self.get(key)
+        value = self.await get(key)
         if value is not None:
             return self.set(key, value, seconds)
         return False
 
     # 装饰器支持
-    def cached(self, ttl: int = None, key_prefix: str = "cache", **kwargs):
+async def cached(self, ttl: int = None, key_prefix: str = "cache", **kwargs):
         """缓存装饰器."""
         if ttl is None:
             ttl = self.config.default_ttl
 
-        def decorator(func):
+async def decorator(func):
             if not self.config.enable_decorators:
                 return func
 
@@ -436,22 +436,22 @@             @cached(
                 ttl=ttl, key_prefix=key_prefix, use_consistency_manager=False, **kwargs
             )
-            def wrapper(*args, **kwargs):
+async def wrapper(*args, **kwargs):
                 return func(*args, **kwargs)
 
             return wrapper
 
         return decorator
 
-    def cache_invalidate(self, pattern: str = None, keys: list[str] = None):
+async def cache_invalidate(self, pattern: str = None, keys: list[str] = None):
         """缓存失效装饰器."""
 
-        def decorator(func):
+async def decorator(func):
             if not self.config.enable_decorators:
                 return func
 
             @cache_invalidate(pattern=pattern, keys=keys)
-            def wrapper(*args, **kwargs):
+async def wrapper(*args, **kwargs):
                 return func(*args, **kwargs)
 
             return wrapper
@@ -459,7 +459,7 @@         return decorator
 
     # 统计和监控
-    def get_stats(self) -> dict[str, Any]:
+async def get_stats(self) -> dict[str, Any]:
         """获取缓存统计信息."""
         stats = {
             "backend": self.config.backend.value,
@@ -500,7 +500,7 @@ 
         return stats
 
-    def health_check(self) -> dict[str, Any]:
+async def health_check(self) -> dict[str, Any]:
         """健康检查."""
         health = {
             "status": "healthy",
@@ -513,7 +513,7 @@         try:
             test_key = "__health_check__"
             self.set(test_key, "test", 1)
-            value = self.get(test_key)
+            value = self.await get(test_key)
             self.delete(test_key)
             if value != "test":
                 health["adapter_healthy"] = False
@@ -541,7 +541,7 @@ _global_cache_manager: UnifiedCacheManager | None = None
 
 
-def get_cache_manager(config: UnifiedCacheConfig = None) -> UnifiedCacheManager:
+async def get_cache_manager(config: UnifiedCacheConfig = None) -> UnifiedCacheManager:
     """获取全局缓存管理器实例."""
     global _global_cache_manager
     if _global_cache_manager is None:
@@ -549,44 +549,44 @@     return _global_cache_manager
 
 
-def reset_cache_manager():
+async def reset_cache_manager():
     """重置全局缓存管理器."""
     global _global_cache_manager
     _global_cache_manager = None
 
 
 # 便捷函数
-def cache_get(key: str, default: Any = None) -> Any:
+async def cache_get(key: str, default: Any = None) -> Any:
     """便捷获取缓存."""
-    return get_cache_manager().get(key, default)
-
-
-def cache_set(key: str, value: Any, ttl: int | None = None) -> bool:
+    return await get_cache_manager().get(key, default)
+
+
+async def cache_set(key: str, value: Any, ttl: int | None = None) -> bool:
     """便捷设置缓存."""
     return get_cache_manager().set(key, value, ttl)
 
 
-def cache_delete(key: str) -> bool:
+async def cache_delete(key: str) -> bool:
     """便捷删除缓存."""
     return get_cache_manager().delete(key)
 
 
-def cache_exists(key: str) -> bool:
+async def cache_exists(key: str) -> bool:
     """便捷检查缓存是否存在."""
     return get_cache_manager().exists(key)
 
 
-def cache_clear() -> None:
+async def cache_clear() -> None:
     """便捷清空缓存."""
     get_cache_manager().clear()
 
 
-def cache_get_many(keys: list[str]) -> dict[str, Any]:
+async def cache_get_many(keys: list[str]) -> dict[str, Any]:
     """便捷批量获取缓存."""
     return get_cache_manager().get_many(keys)
 
 
-def cache_set_many(mapping: dict[str, Any], ttl: int | None = None) -> bool:
+async def cache_set_many(mapping: dict[str, Any], ttl: int | None = None) -> bool:
     """便捷批量设置缓存."""
     return get_cache_manager().set_many(mapping, ttl)
 
