--- a/src/services/content_analysis.py+++ b/src/services/content_analysis.py@@ -35,7 +35,7 @@     pass  # 添加pass语句
     """内容类"""
 
-    def __init__(self, content_entity_id: str, content_type: str, data: dict[str, Any]):
+async def __init__(self, content_entity_id: str, content_type: str, data: dict[str, Any]):
         """函数文档字符串."""
         pass  # 添加pass语句
         self.id = content_entity_id
@@ -49,7 +49,7 @@     pass  # 添加pass语句
     """用户配置文件类"""
 
-    def __init__(self, user_entity_id: str, preferences: dict[str, Any] = None):
+async def __init__(self, user_entity_id: str, preferences: dict[str, Any] = None):
         """函数文档字符串."""
         pass  # 添加pass语句
         self.user_id = user_entity_id
@@ -62,7 +62,7 @@     pass  # 添加pass语句
     """分析结果类"""
 
-    def __init__(
+async def __init__(
         self,
         entity_id: str = "",
         analysis_type: str = "",
@@ -82,7 +82,7 @@ class ContentAnalysisService(SimpleService):
     """内容分析服务."""
 
-    def __init__(self):
+async def __init__(self):
         """函数文档字符串."""
         pass  # 添加pass语句
         super().__init__("ContentAnalysisService")
@@ -124,14 +124,14 @@         # 实现内容分析逻辑
         # 这里提供基本的文本分析功能,生产环境可扩展为ML模型分析
         if content.content_type == "text":
-            text_analysis = self.analyze_text(content.data.get("text", ""))
+            text_analysis = self.await analyze_text(content.data.get("text", ""))
             analysis_data = {
-                "sentiment": text_analysis.get("sentiment", "neutral"),
-                "keywords": text_analysis.get("keywords", []),
-                "category": self._categorize_content(content.data.get("text", "")),
+                "sentiment": text_analysis.await get("sentiment", "neutral"),
+                "keywords": text_analysis.await get("keywords", []),
+                "category": self.await _categorize_content(content.data.get("text", "")),
                 "quality_score": self._calculate_quality_score(content),
-                "language": text_analysis.get("language", "zh"),
-                "word_count": text_analysis.get("word_count", 0),
+                "language": text_analysis.await get("language", "zh"),
+                "word_count": text_analysis.await get("word_count", 0),
             }
         else:
             # 非文本内容的默认分析
@@ -160,7 +160,7 @@                 results.append(result)
         return results
 
-    def _categorize_content(self, text: str) -> str:
+async def _categorize_content(self, text: str) -> str:
         """内容分类."""
         football_keywords = [
             "足球",
@@ -180,10 +180,10 @@             return "足球新闻"
         return "一般内容"
 
-    def _calculate_quality_score(self, content: Content) -> float:
+async def _calculate_quality_score(self, content: Content) -> float:
         """计算内容质量分数."""
         if content.content_type == "text":
-            text = content.data.get("text", "")
+            text = content.data.await get("text", "")
             # 基于长度,关键词数量等计算质量分数
             word_count = len(text.split())
             keyword_count = len([kw for kw in ["足球", "比赛", "分析"] if kw in text])
@@ -194,7 +194,7 @@             return min(base_score + keyword_bonus + length_bonus, 1.0)
         return 0.5
 
-    def analyze_text(self, text: str) -> dict:
+async def analyze_text(self, text: str) -> dict:
         """分析文本内容 - 同步版本用于测试."""
         if not text:
             return {"error": "Empty text"}
@@ -210,7 +210,7 @@             "summary": self.generate_summary(text),
         }
 
-    def extract_entities(self, text: str) -> list:
+async def extract_entities(self, text: str) -> list:
         """提取实体."""
         # 简单的实体提取逻辑
         entities = []
@@ -236,7 +236,7 @@ 
         return entities[:10]  # 限制返回数量
 
-    def classify_content(self, content: str) -> dict:
+async def classify_content(self, content: str) -> dict:
         """内容分类."""
         if not content:
             return {"category": "unknown", "confidence": 0.0}
@@ -269,7 +269,7 @@             }
         return {"category": "general", "confidence": 0.5}
 
-    def analyze_sentiment(self, text: str) -> dict:
+async def analyze_sentiment(self, text: str) -> dict:
         """情感分析."""
         if not text:
             return {"sentiment": "neutral", "score": 0.0}
@@ -300,7 +300,7 @@             "negative_count": neg_count,
         }
 
-    def generate_summary(self, text: str, max_length: int = 100) -> str:
+async def generate_summary(self, text: str, max_length: int = 100) -> str:
         """生成摘要."""
         if not text or len(text) <= max_length:
             return text or ""
