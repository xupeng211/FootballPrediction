# L2数据回填脚本依赖
# L2 Data Backfill Script Dependencies

# 核心异步依赖
aiofiles>=23.0.0
aiohttp>=3.8.0

# 进度条和CLI
tqdm>=4.64.0

# HTTP客户端 (如果需要独立使用)
httpx>=0.24.0

# 重试机制
tenacity>=8.2.0

# 项目内部依赖 (开发时)
# 这些在项目环境中已经可用
# - src.collectors.l2_fetcher
# - src.collectors.l2_parser
# - src.schemas.l2_schemas