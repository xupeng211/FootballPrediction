/**
 * Recon 映射存储入口薄壳。
 *
 * 复杂实现迁移到 `ReconMappingStoreImpl.js`，该文件仅保留
 * 稳定导出职责，用于核心门禁收口。
 *
 * @module infrastructure/services/recon/ReconMappingStore
 */

'use strict';

module.exports = require('./ReconMappingStoreImpl');
