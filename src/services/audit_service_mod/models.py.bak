from typing import Any

"" 审计服务模型(兼容版本)
Audit Service Models (Compatibility Version)
"" from dataclasses import dataclassfrom datetime import datetime

from enum import Enum


class AuditAction(Enum):
:
    """审计动作"" CREATE = "create UPDATE = "update DELETE = "delete READ = "read LOGIN = "login LOGOUT = "logout EXPORT = "export class AuditSeverity(Enum):"""
:
    """审计严重级别"" LOW = "low MEDIUM = "medium HIGH = "high CRITICAL = "critical @dataclassclass AuditEvent:"""

    """审计事件"""
    id: straction: AuditAction

    severity: AuditSeverityuser_id: str

    resource_type: strresource_id: str | None

    message: strtimestamp: datetime

    metadata: dict[str, Any] | None = None

    def __post_init__(self) -> None:
        if:self.metadata is Noneself.metadata = {}:
