# Inference Service Dependencies
# 推理服务新增依赖包

# File System Monitoring (for hot reload)
watchdog>=3.0.0

# Caching and Performance
cachetools>=5.3.0
redis[hiredis]>=4.5.0

# Machine Learning (required for inference)
scikit-learn>=1.3.0
joblib>=1.3.0

# Async Utilities
concurrent-futures>=3.1.1

# Data Validation (already in main requirements but ensuring latest)
pydantic>=2.5.0

# Feature Engineering
featuretools>=1.28.0

# Optional: MLflow (for experiment tracking)
# mlflow>=2.22.2

# Optional: Optuna (for hyperparameter optimization)
# optuna>=4.6.0

# Optional: TensorFlow/LSTM (if using deep learning models)
# tensorflow>=2.18.0

# Development and Testing (in addition to requirements-dev)
pytest-asyncio>=0.21.0
pytest-mock>=3.14.0
factory-boy>=3.3.1

# Code Quality
mypy>=1.18.2
types-redis>=4.6.0
types-cachetools>=5.3.0